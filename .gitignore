node_modules
dist
.next
.turbo
.env
.env.production
.env.*.local
# .env.local ของ Next.js — pattern .env.*.local ด้านบนไม่ครอบ (ไม่มีส่วนกลาง)
.env.local
docker/.env
*.log
.expo
apps/api/uploads/*
!apps/api/uploads/.gitkeep
apps/api/private-uploads/*
!apps/api/private-uploads/.gitkeep

.pnpm-store
*.tsbuildinfo

# symlink ของ skills ที่ npx skills add สร้างให้ — ชี้ด้วย absolute path ของเครื่องที่ลง
# ตัว skill จริงอยู่ใน .agents/skills/ ซึ่ง commit ไว้แล้ว ใครโคลนไปรัน `npx skills add` เองจะได้ symlink ของตัวเอง
.claude/skills/

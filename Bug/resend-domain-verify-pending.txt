[2026-07-25] Resend domain verify ค้าง — พักงานไว้ก่อน (ยังไม่ใช่บั๊กโค้ด แค่ setup ค้างรอ user ทำ DNS)
==========================================================

สถานะตอนนี้
-----------
- โดเมน hoprak.com ถูกเพิ่มใน Resend แล้ว (resend.com/domains) — status: pending
- Resend เช็ค DNS แล้วไม่เจอ record เลยสักตัว: "Domain not found: This domain wasn't found in any DNS servers yet"
- DNS ของ hoprak.com จัดการอยู่ที่ Hostatom (cPanel) — ยังไม่ได้เพิ่ม record ที่ฝั่งนั้นเลย
- ระหว่างนี้ระบบยังใช้ dev fallback ได้ปกติ: ส่งอีเมลจริงไม่ได้เมื่อไหร่ (SMTP reject/ไม่ตั้งค่า)
  MailService.send() คืน false แล้ว OwnerApplicationsService log รหัส OTP ไว้ใน server console แทน
  (ดู Bug/bugs.txt [27][28] ประกอบ) — ทดสอบ flow partner-register ได้ครบไม่ติดอะไร

DNS record ที่ต้องเพิ่มที่ Hostatom (cPanel > Zone Editor > hoprak.com)
-----------------------------------------------------------------------
[อัปเดต 27/07/2026 — ลบ domain entry เก่า (hopak.co.th ผิด) ใน Resend ทิ้งแล้ว
สร้างใหม่เป็น hoprak.com ได้ DKIM key ใหม่ ใช้ชุดค่าด้านล่างนี้เท่านั้น ของเก่าเลิกใช้]
1. TXT   Name: resend._domainkey   Content: p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC/0pic1Z/2hZy6cTFo5S2Z4ApRlBBMoz45nF8CER3cwdgWKU48Ak0V6FBu+hJ9ctVpVNHY7wzmnkRq6WMCDAjVxW7KXFMyPOdW/aSdUHYROiq/mugZ09dOJP7DVXCcyNh/xg9kI/YNZKa8dA4ml2hdQx3kKU+sCIDCw2lyg9ZlKQIDAQAB
2. MX    Name: send   Content: feedback-smtp.ap-northeast-1.amazonses.com   Priority: 10
3. TXT   Name: send   Content: v=spf1 include:amazonses.com ~all
4. TXT (optional)   Name: _dmarc   Content: v=DMARC1; p=none;

หมายเหตุ: record ทั้งหมดอยู่ใต้ subdomain "send"/"resend._domainkey" ไม่แตะ root
(hoprak.com) เลย — เว็บหลักไม่กระทบ ไม่ต้องซื้อโดเมนเพิ่มสำหรับเรื่องนี้
(user เคยถามเรื่องซื้อโดเมนเพิ่ม — ตอบไปแล้วว่าไม่จำเป็น)

ขั้นตอนตอนกลับมาทำต่อ
----------------------
1. เข้า cPanel ของ hoprak.com ผ่าน Hostatom client area
2. หาเมนู Zone Editor (หมวด Domains) > Manage > Add Record
3. ใส่ 3-4 record ตามตารางข้างบน (ระวังช่อง Name บาง panel เติม .hoprak.com ให้อัตโนมัติ
   — ถ้าเติมซ้ำให้ลบส่วนท้ายออก, DKIM TXT ยาวมากบาง panel ตัดที่ 255 ตัวอักษร ถ้าใส่ไม่ผ่านต้องแก้)
4. กลับไป resend.com/domains กด Verify
5. Verify ผ่านแล้ว แก้ apps/api/.env: เปลี่ยน SMTP_FROM จาก onboarding@resend.dev
   เป็นอีเมลในโดเมนตัวเอง เช่น noreply@hoprak.com แล้ว restart API server
6. ทดสอบส่ง OTP ไปอีเมลที่ไม่ใช่ karshi0234@gmail.com จริง ยืนยันว่าไม่ใช่ 550 sandbox error อีก

สถานะ: ยังไม่แก้ — รอ user เข้า Hostatom เพิ่ม DNS record เอง (ผมทำแทนไม่ได้ ไม่มีสิทธิ์เข้า DNS panel)

Hopak — บันทึกบั๊กที่เจอ
==========================

[แก้แล้ว] Role case-mismatch — RolesGuard ปฏิเสธทุก request
--------------------------------------------------------
ไฟล์: apps/api/src/modules/auth/auth.service.ts
อาการ: JWT เก็บ role ตัวใหญ่ตาม Prisma enum (TENANT/OWNER/ADMIN)
แต่ @Roles() decorator เช็คตัวเล็ก (tenant/owner/admin) ตาม @hopak/shared
ผล: ทุก endpoint ที่มี RolesGuard ตอบ 403 Forbidden ตลอด
(จองห้อง, ยืนยันจอง, อนุมัติหอ ฯลฯ ใช้งานไม่ได้เลย)
แก้ไข: sign() แปลง role เป็น .toLowerCase() ก่อนใส่ JWT
สถานะ: แก้แล้ว + ทดสอบ booking flow เต็มรอบผ่าน (16/07/2026)

[แก้แล้ว] ไม่มีการเช็ค/hash password จริง — ช่องโหว่ความปลอดภัยร้ายแรง
--------------------------------------------------------
ไฟล์: apps/api/prisma/schema.prisma, apps/api/src/modules/auth/auth.service.ts,
apps/api/src/modules/users/users.service.ts,
apps/api/src/modules/admin/admins/admins.service.ts,
apps/api/src/modules/admin/approvals/approvals.service.ts,
apps/api/prisma/seed.ts
อาการ: User model ใน Prisma ไม่มี field password เลย login() หา user จาก
email/phone อย่างเดียว ไม่เช็ค password ที่ส่งมา — ใครก็ login ด้วย email
ไหนก็ได้โดยไม่ต้องรู้รหัสผ่านจริง
แก้ไข:
- เพิ่ม password String? ใน User model + migrate (add-user-password)
- register() hash ด้วย bcryptjs (10 rounds) ก่อนเก็บ, login() เทียบด้วย
  bcrypt.compare จริง, ปฏิเสธถ้า user ไม่มี password (เช่น Google-only account)
- ใช้ bcryptjs แทน bcrypt (native) เลี่ยงปัญหา compile บน Windows
- ระหว่างแก้เจอเพิ่ม: user object (รวม password hash) ถูกส่งกลับตรงๆ ใน
  response หลายจุด (auth.service register/login, users.service findById/
  updateProfile/becomeOwner, admins.service list ใช้ include:{user:true},
  approvals.service listPending ใช้ include:{owner:true}) — แก้ให้ใช้
  select เฉพาะ field ที่ปลอดภัย/omitPassword() ทุกจุด ไม่ให้ hash รั่วออกไป
- seed.ts: ตั้ง password ทดสอบ "hopak1234" ให้ account seed (admin/owner)
สถานะ: แก้แล้ว + ทดสอบผ่านครบ 5 เคส (16/07/2026): register ไม่รั่ว password,
login รหัสถูก 201, รหัสผิด 401, account เก่าที่ไม่มี password ถูกล็อก 401
(ถูกต้อง — ต้อง reset ผ่านช่องทางอื่น), seed admin login ด้วยรหัสใหม่ 201

[gap] /admin/users endpoint ไม่มีจริงใน backend
--------------------------------------------------------
ไฟล์: apps/web/src/app/(admin)/admin/users/page.tsx
อาการ: หน้าเว็บแอดมิน "ผู้ใช้" เรียก GET /admin/users
แต่ apps/api ยังไม่มี endpoint นี้ (มีแต่ /admin/admins, /admin/approvals,
/admin/finance, /admin/analytics)
ผล: หน้า /admin/users โหลดไม่ได้ข้อมูล (error เงียบ, users เป็น [])
สถานะ: ยังไม่แก้ — ต้องเพิ่ม endpoint listing users ทั้งหมดในฝั่ง admin module

[gap] Partner dashboard เรียก endpoint admin-only — owner ไม่มีสิทธิ์เห็นข้อมูล
--------------------------------------------------------
ไฟล์: apps/web/src/app/(partner)/partner/dashboard/page.tsx,
apps/api/src/modules/admin/finance/finance.controller.ts
อาการ: หน้า partner/dashboard เรียก GET /admin/finance/summary
แต่ endpoint นี้ guard ด้วย @Roles('admin') เท่านั้น เจ้าของหอ (owner) โดน 403
ทุกครั้ง (เงียบ เพราะมี .catch แล้วโชว์ 0 แทน error)
ผล: เจ้าของหอไม่เห็นรายได้/ยอดจองของตัวเองในแดชบอร์ดเลย
ต้นตอ: backend ไม่มี endpoint สำหรับ owner ดูสรุปยอดจอง/รายได้เฉพาะหอตัวเอง
(bookings.service listForTenant กรองด้วย tenantId เท่านั้น ไม่มี listForOwner)
สถานะ: ยังไม่แก้ — ต้องเพิ่ม endpoint ฝั่ง owner (เช่น GET /partner/summary)
ที่กรองด้วย ownerId แทน

[แก้แล้ว] Room/Dorm status case-mismatch — การ์ดหอพักขึ้น "ห้องเต็ม" ทั้งที่มีห้องว่าง
--------------------------------------------------------
ไฟล์: apps/web/src/app/(tenant)/search/page.tsx, apps/web/src/app/(tenant)/dorms/[id]/page.tsx
อาการ: Prisma ส่ง Room.status เป็นตัวใหญ่ (AVAILABLE/OCCUPIED) ตาม enum จริง
แต่ frontend filter เทียบกับ '@hopak/shared' Room type ที่นิยามตัวเล็ก (available/occupied)
ผล: r.status === 'available' ไม่ match กับข้อมูลจริงจาก API เลยสักครั้ง
การ์ดค้นหาขึ้น "ห้องเต็ม" ตลอด แม้หอมีห้องว่างจริง (เหมือนบั๊ก role case-mismatch ก่อนหน้า
แต่รอบนี้อยู่ฝั่ง data ไม่ใช่ auth)
แก้ไข: เทียบแบบ case-insensitive (.toUpperCase() === 'AVAILABLE'/'AIR') ในหน้า
search และ dorms/[id]
สถานะ: แก้แล้วในหน้า search + dorms/[id] — ยังไม่เช็ค partner/rooms ว่ากระทบเหมือนกันไหม
(ทางแก้ระยะยาว: ให้ backend normalize enum เป็น lowercase ก่อนส่งออก จะได้ไม่ต้องแก้ทุกจุดที่ใช้)

[แก้แล้ว] profile page เช็ค role ตัวเล็กแต่ API ส่งตัวใหญ่ — ปุ่ม "เปิดหอพักกับ Hopak" ไม่โผล่
--------------------------------------------------------
ไฟล์: apps/web/src/app/(tenant)/profile/page.tsx
อาการ: user.role === 'tenant' เทียบตัวเล็ก แต่ GET /users/me ส่ง role ตรงจาก
Prisma enum ('TENANT' ตัวใหญ่) — เงื่อนไขไม่ match เลยสักครั้ง (บั๊กรูปแบบเดียวกับ
role case-mismatch ใน auth และ room/dorm status ก่อนหน้า — เกิดซ้ำเพราะ backend
ส่ง enum ดิบไม่แปลงเป็น lowercase ก่อนส่งออกทุก endpoint)
ผล: ผู้เช่าไม่เห็นปุ่ม "เปิดหอพักกับ Hopak" เลย เปิดเป็นเจ้าของหอไม่ได้ผ่าน UI
แก้ไข: เทียบ user.role.toLowerCase() === 'tenant' + เชื่อม flow ให้หลังกด
becomeOwner แล้ว redirect ไป /partner/dorms/new ทันที (แทนที่จะ refetch เฉยๆ)
สถานะ: แก้แล้ว
หมายเหตุ: ควรพิจารณาแก้ที่ต้นตอ (normalize role/status enum เป็น lowercase
ที่ response interceptor ฝั่ง backend ทีเดียว) แทนแก้เป็นจุดๆ ในทุกหน้าเว็บ

[แก้แล้ว] Booking status case-mismatch กระจายหลายหน้า (timeline/label/filter ไม่ทำงาน)
--------------------------------------------------------
ไฟล์: apps/web/src/app/(tenant)/bookings/[id]/page.tsx, bookings/page.tsx,
(admin)/admin/bookings/page.tsx, (partner)/partner/requests/page.tsx,
(partner)/partner/slips/page.tsx
อาการ: booking.status จาก API เป็นตัวใหญ่ (PENDING/CONFIRMED/...) แต่ทุกหน้า
เทียบ/lookup ด้วยตัวเล็ก (STEPS indexOf, STATUS_LABEL[status], filter === 'pending')
ผล: timeline ไม่ไฮไลท์ขั้นไหนเลย, ป้ายสถานะว่างเปล่า (undefined), ปุ่มชำระเงิน/ยืนยัน
ไม่โผล่, ตัวกรองสถานะใน admin ไม่ทำงาน, requests/slips filter ไม่เจอ booking เลย
สักตัว — บั๊กรูปแบบเดียวกับ role/room-status ซ้ำเป็นครั้งที่ 4
แก้ไข: เพิ่ม apps/web/src/lib/normalize.ts (normalizeStatus) แล้วเรียกใช้ตอน
compare/lookup ทุกจุดที่ระบุด้านบน
สถานะ: แก้แล้ว

[gap ร้ายแรง] GET /bookings กรองด้วย tenantId เสมอ ไม่สนใจ role ผู้เรียก
--------------------------------------------------------
ไฟล์: apps/api/src/modules/bookings/bookings.controller.ts,
apps/api/src/modules/bookings/bookings.service.ts (listForTenant)
อาการ: endpoint เดียวใช้ทั้ง tenant/owner/admin แต่ service กรองด้วย
tenantId=user.id ตายตัว ไม่เช็ค role เลย
ผล: partner/requests (คำขอจอง) กับ admin/bookings (การจองทั้งหมด) เห็นข้อมูลผิด
คนละชุด — owner/admin เห็นแค่ booking ที่ตัวเองเคยจองในฐานะผู้เช่าเท่านั้น
(ปกติมักจะว่างเปล่า) ไม่ใช่คำขอจองที่เข้ามาที่หอตัวเอง/ทั้งระบบ
สถานะ: ยังไม่แก้ — ต้องเพิ่ม logic ฝั่ง backend: admin เห็นทั้งหมด,
owner เห็นเฉพาะ booking ของห้องในหอที่ตัวเองเป็นเจ้าของ, tenant เห็นของตัวเอง
(เกี่ยวโยงกับ gap "partner dashboard data" ที่บันทึกไว้ก่อนหน้า — ควรแก้พร้อมกัน)

[แก้แล้ว] partner/rooms ใช้กรอก Dorm ID มือ + room.status case-mismatch อีกจุด
--------------------------------------------------------
ไฟล์: apps/web/src/app/(partner)/partner/rooms/page.tsx
อาการ: หน้าเดิมให้ owner กรอก Dorm ID เองแบบ raw text (ใช้งานจริงไม่ได้ ไม่มีทาง
รู้ dormId ของตัวเอง) + toggleStatus เทียบ current==='available' ตัวเล็ก
(บั๊กเคสเดียวกับที่เจอซ้ำหลายจุด)
ผล: หน้าจัดการห้องใช้งานจริงไม่ได้เลย ต้องรู้ dormId ล่วงหน้า
แก้ไข: เปลี่ยนเป็น dropdown เลือกจาก GET /dorms/mine (endpoint ใหม่ที่เพิ่มพร้อม
แก้ partner-dashboard gap) + ใช้ normalizeStatus() เทียบสถานะ
สถานะ: แก้แล้ว

[แก้แล้ว] Partner dashboard / GET bookings ผิด scope (ต่อจาก gap ที่บันทึกไว้ก่อนหน้า)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/bookings/bookings.controller.ts,
apps/api/src/modules/bookings/bookings.service.ts,
apps/api/src/modules/dorms/dorms.controller.ts, dorms.service.ts,
apps/web/src/app/(partner)/partner/dashboard/page.tsx
แก้ไข:
- เพิ่ม bookingsService.listForOwner()/listAll() แล้วให้ GET /bookings
  เช็ค role: admin เห็นทั้งหมด, owner เห็นเฉพาะ booking ของหอตัวเอง, tenant เห็นของตัวเอง
- เพิ่ม GET /dorms/mine (owner-only) คืนหอ+ห้องของเจ้าของที่ login อยู่
- partner/dashboard คำนวณรายได้/ห้องทั้งหมด/ห้องว่างจากข้อมูลจริงที่ scope ถูกแล้ว
สถานะ: แก้แล้ว — ทำให้ partner/requests, partner/slips, admin/bookings,
partner/dashboard เห็นข้อมูลถูก scope ทั้งหมดพร้อมกัน (root cause เดียวกัน)

[แก้แล้ว] partner/slips ลิงก์ไปหน้า PDF ที่ไม่มีจริง (dead link)
--------------------------------------------------------
ไฟล์: apps/web/src/app/(partner)/partner/slips/page.tsx
อาการ: ปุ่ม "ปริ้น PDF" เดิมลิงก์ไป /api/documents/booking-slip/:id ซึ่งไม่มี
route นี้อยู่จริงทั้งฝั่งเว็บ (ไม่มี proxy) และฝั่ง api (PdfService มีแต่ยังไม่ผูก
controller/route ใดๆ)
ผล: กดแล้วเจอ 404 เงียบๆ
แก้ไข: เปลี่ยนเป็นปุ่ม disabled พร้อม title อธิบายว่ายังไม่เชื่อมระบบ PDF จริง
(ตรงไปตรงมากว่าลิงก์เสีย) — เช่นเดียวกับปุ่ม Export PDF ในหน้า admin/finance
สถานะ: แก้แล้ว (UI ไม่หลอกผู้ใช้ว่าใช้งานได้) — งานจริง (ผูก PdfService เข้า
controller + route) ยังไม่ทำ

[ยังไม่ทดสอบ]
--------------------------------------------------------
- Realtime WebSocket (booking:new event ฝั่ง partner/requests)
- PDF generation (documents module)
- Uploads (S3)
- Google OAuth จริง (ตอนนี้ใช้ placeholder clientID กัน crash)

[14] ไม่มี Navbar ในหน้า tenant/public (พบตอน "ตอนนี้ระบบถึงไหนเเล้ว")
- ปัญหา: (tenant) และ (auth) route group ไม่มี layout.tsx เลย หน้าแรก/search/booking/profile เลยไม่มีเมนู login, register, ค้นหา, ออกจากระบบ ทั้งที่ (admin)/(partner) มี sidebar แล้ว
- แก้: สร้าง apps/web/src/components/Navbar.tsx (เช็ค token, โชว์ role-aware dashboard link, login/logout) + เพิ่ม layout.tsx ให้ (tenant) และ (auth) group + เติม Navbar เข้า root page.tsx (home อยู่นอก group)
- สถานะ: แก้แล้ว

[15] /search พัง 500 หลังรัน `next build` ทับตอน dev server ยังรันอยู่
- ปัญหา: รัน `next build` (production build เพื่อเช็ค type error) ขณะ dev server เดิม (PID 896) ยังใช้ .next/ อยู่ ทำให้ webpack chunk เพี้ยน (Cannot find module './192.js')
- แก้: ลบ apps/web/.next ทิ้ง แล้ว kill dev server เก่า restart `next dev` ใหม่ทั้งหมด
- ป้องกันครั้งหน้า: ห้ามรัน `next build` ขณะ dev server ตัวเดียวกันกำลังรันอยู่ ใช้คนละ terminal/คนละเวลา หรือลบ .next ก่อน restart ทุกครั้งหลัง build
- สถานะ: แก้แล้ว

[16] Dev server jest-worker crash ชั่วคราวตอนคอมไพล์ /dorms/[id] ครั้งแรกหลัง redesign
- อาการ: "Error: Jest worker encountered 2 child process exceptions, exceeding retry limit" ทำให้ /dorms/x ตอบ 500 ครั้งแรก
- สาเหตุ: worker process ของ Next.js dev server ล่มชั่วคราว (มักเกิดจาก resource ตอน compile หน้าใหญ่ครั้งแรกบน Windows) ไม่ใช่บัคโค้ด
- แก้: restart dev server ใหม่ ครั้งที่ 2 compile ผ่านปกติ (200)
- หมายเหตุ: ถ้าเจอ error นี้อีกให้ restart ก่อน ไม่ต้องแก้โค้ด

[17] Partner "คำขอจอง" ไม่มีปุ่มปฏิเสธจริง (mockup มีปุ่ม ปฏิเสธ แต่ backend ไม่มี endpoint)
- ปัญหา: /bookings/:id/cancel เช็ค tenantId เท่านั้น เจ้าของหอเรียกไม่ได้ (403) ไม่มีทางปฏิเสธคำขอจองที่ค้างอยู่
- แก้: เพิ่ม PATCH /bookings/:id/reject (@Roles('owner')) เช็ค dorm ownership, transition pending->cancelled เหมือน cancel แต่ไม่เช็ค cancel-window เพราะเป็นฝั่งเจ้าของหอปฏิเสธ ไม่ใช่ผู้เช่ายกเลิก
- สถานะ: แก้แล้ว (bookings.service.ts, bookings.controller.ts, partner/requests page ต่อปุ่มจริง)

[18] Admin finance summary รวม SETTLED+TRANSFERRED เข้าด้วยกัน ไม่แยกยอด "โอนแล้ว" กับ "ค้างโอน"
- ปัญหา: ไม่มีทางแสดง KPI "ยอดเข้าบัญชีส่วนกลาง / คงค้างรอโอน" ตาม mockup เพราะ service คืนแค่ totalCommission/totalPayout รวมทุกสถานะ
- แก้: เพิ่ม totalReceived, totalTransferred, totalPending แยกตาม Payment.status ใน finance.service.ts summary()
- สถานะ: แก้แล้ว

[19] npm install -g railway ผิดแพ็กเกจ + PATH เครื่อง user ไม่มี npm global bin เลย
- ปัญหา: (1) `npm i -g railway` ลง package ผิด (ของจริงคือ @railway/cli) รันคำสั่ง railway ไม่เจอ (2) แม้ลงถูกแล้ว ก็ยังเรียกไม่ได้เพราะ %APPDATA%\npm ไม่อยู่ใน User PATH เลย (ปัญหาถาวรของเครื่อง ไม่เกี่ยวกับแพ็กเกจนี้อย่างเดียว - CLI tool ที่ลงด้วย npm -g ตัวอื่นก็ใช้ไม่ได้เหมือนกัน)
- แก้: uninstall railway (ผิด) -> install @railway/cli (ถูก) -> เพิ่ม %APPDATA%\npm เข้า User PATH ถาวรด้วย [Environment]::SetEnvironmentVariable + เพิ่ม session ปัจจุบันด้วย $env:PATH
- สถานะ: แก้แล้ว, railway --version รันได้ปกติ (5.27.0) ต้องเปิด terminal ใหม่ถึงจะเห็น PATH ถาวรใน session อื่น

[20] Jest worker crash ("exceeding retry limit") ตอนเปิดหน้าเว็บ + error log "Cannot read properties of null (reading 'useContext')" ตามมา
- ปัญหา: dev server เดิมค้าง state เสีย (jest-worker child process ตายซ้ำเกิน retry limit) หลัง request ถูก abort กลางคัน ("The user aborted a request.") ทำให้ Next พยายาม render internal ErrorBoundary เพื่อ log error แต่ตัว ErrorBoundary เรียก usePathname() นอก component tree ที่มี context ทำให้ throw ซ้อนอีกชั้น (log อย่างเดียว ไม่ทำหน้าเว็บพัง)
- แก้: kill process เดิม (PID บน port 3001), rm -rf apps/web/.next, รัน pnpm dev ใหม่ - รูปแบบเดิมกับบัค [16] (ปัญหา dev-server ค้าง ไม่ใช่บัคโค้ด)
- พบเพิ่ม: Bash tool session เอง PATH ก็ไม่มี %APPDATA%\npm เหมือนกัน (คนละ session กับ PowerShell ที่แก้ไปแล้วใน [19]) - ต้อง export PATH เพิ่ม npm global bin ทุกครั้งที่เปิด Bash ใหม่เพื่อเรียก pnpm ได้
- สถานะ: แก้แล้ว, home/search คืน 200 หลัง restart

[21] useDormSearch ส่ง province/university=undefined เป็น string จริง ทำ filter "ทั้งหมด" อาจไม่คืนหอที่ไม่ตรงเงื่อนไข
- ปัญหา: new URLSearchParams({ province: undefined }) ใน JS coerce ค่า undefined เป็น string "undefined" จริง (ยืนยันด้วย node -e) ทำให้ query ที่ส่งไป backend กลายเป็น ?province=undefined แทนที่จะไม่ส่ง key เลย ฝั่ง service ทำ where: { province: "undefined" } กรองไม่ตรงกับหอพักไหนเลย
- แก้: เขียน useDormSearch ใหม่ ใช้ params.set() แค่ตอนมีค่าจริงเท่านั้น (ไม่ผ่าน object ที่มี key เป็น undefined เข้า URLSearchParams ตรงๆ) พร้อมเพิ่ม q (ค้นหาชื่อ/จังหวัด/มหาลัยรวม) รองรับช่องค้นหาใน Navbar ใหม่
- สถานะ: แก้แล้ว (useDormSearch.ts, search-query.dto.ts เพิ่ม q, dorms.service.ts เพิ่ม OR contains)

[22] Login page ถูกไฟล์นอก (IDE/linter tool) แทนที่ทั้งหมดด้วย mock component ที่พังทั้งเว็บ
- ปัญหา: มีเครื่องมือภายนอกแก้ apps/web/src/app/(auth)/login/page.tsx เขียนทับของจริงทั้งหมด (comment โค้ดเดิมทิ้ง) ด้วย preview component ปลอม ที่ (1) ไม่มี 'use client' (comment ทิ้งไปด้วย) ทำ Next โยน error "importing component that needs useState... Server Components by default" พังทั้ง /login และลามไป GET / 500 ด้วย (2) ใช้ mockApi.post ปลอมแทน apiClient จริง ล็อกอินสำเร็จปลอมทุกครั้งที่ email มีค่า+password>=4 ตัว ไม่เซ็ต token จริง ไม่ redirect จริง (3) มี TS error 5 ตัว (implicit any, useState(null) ไม่ type, accessToken บน unknown)
- แก้: เขียนใหม่เก็บ UI/เลย์เอาต์ใหม่ตามที่ user เลือก (เก็บ UI ใหม่ ต่อ backend จริง) แต่ใส่ 'use client' คืน, ต่อ apiClient.post('/auth/login') จริง, setToken+router.push('/') จริง, Google button ใช้ href จริงไป /auth/google, ลบ mockApi/loggedIn-preview-screen ออกหมด
- สถานะ: แก้แล้ว, tsc --noEmit ผ่าน 0 error, home+login คืน 200, ตรวจ log ยืนยันไม่มี error ซ้ำ

[23] Admin/Owner console เข้าไม่ได้เลยจริงๆ ผ่าน middleware ที่เช็ค cookie ซึ่งไม่มีใครเซ็ตเลย
- ปัญหา: middleware.ts เช็ค request.cookies.get('hopak_token') เพื่อ gate /admin/* และ /partner/* แต่ setToken() ใน lib/auth.ts เซ็ตแค่ localStorage อย่างเดียว ไม่เคยเซ็ต cookie ชื่อนี้เลยที่ไหนในโค้ดทั้งหมด (grep ทั้ง backend/frontend ไม่เจอ) ผลคือ middleware redirect ไป /login ทุกครั้งแบบไม่มีเงื่อนไข แม้ล็อกอินเป็น admin/owner จริงและมี token ใน localStorage ถูกต้องก็ตาม - เข้าคอนโซลผ่าน URL ไม่ได้เลย
- พบเพิ่ม: (admin)/admin/layout.tsx และ (partner)/partner/layout.tsx เองก็ไม่มี client-side auth/role guard เลย (ถ้าไม่มี middleware ตัวนี้ ใครก็เปิดดู shell คอนโซลได้แม้ไม่ได้ล็อกอิน หรือล็อกอินผิด role ก็เห็น dashboard ว่างเปล่าค้างตลอดไป ไม่ redirect) - backend เองปลอดภัย (RolesGuard บล็อก 401/403 ถูกต้องทุก endpoint) แต่ UI ฝั่งหน้าไม่มีการ์ดเลย
- แก้: (1) setToken()/clearToken() ใน auth.ts เซ็ต/ลบ cookie hopak_token คู่กับ localStorage ด้วย (2) เพิ่ม useCurrentUser hook + client guard ใน admin+partner layout เช็ค role จริง ไม่ตรง redirect /login พร้อม PageLoader ระหว่างเช็ค
- สถานะ: แก้แล้ว, ยืนยันด้วย curl: ไม่มี cookie -> 307 ไป login, มี cookie (token จริง) -> 200 เข้าได้, tsc ผ่าน 0 error

[24] หน้าฝั่ง tenant หลายหน้า throw "Unhandled Runtime Error: Unauthorized" ถ้าเข้าโดยไม่ได้ล็อกอิน (หรือ token หมดอายุ)
- ปัญหา: useBookings.ts, notifications/page.tsx, profile/page.tsx, bookings/[id]/page.tsx, bookings/[id]/pay/page.tsx, useFavorites.ts (toggle) เรียก apiClient.get/post แล้วต่อ .then(...) โดยไม่มี .catch เลย พอ backend คืน 401 (ไม่มี token หรือ token หมดอายุ) promise reject ไม่มีใครจับ กลายเป็น unhandled rejection ทำ Next dev โชว์ full-page error overlay แทนที่จะ redirect ไป login เฉยๆ - เกิดขึ้นจริงตอนกด "การจองของฉัน" ใน navbar (ลิงก์โชว์ตลอดไม่ว่าล็อกอินหรือไม่ตาม design ใหม่) ทั้งที่หน้า /bookings มี router.replace('/login') guard อยู่แล้ว แต่ useBookings hook เองยัง fetch แบบไม่ป้องกันคู่ขนานกัน เลย throw ก่อน redirect จะทันเกิด
- แก้: เพิ่ม .catch() ทุกจุดที่ขาด (hooks + pages) และเพิ่ม guard เช็ค getToken() ก่อน fetch พร้อม router.replace('/login') ใน notifications, profile, bookings/[id], bookings/[id]/pay (bookings list กับ saved มี guard อยู่แล้วจากรอบก่อน แค่เพิ่ม catch ให้ hook)
- สถานะ: แก้แล้ว, tsc --noEmit ผ่าน 0 error

[25] สมัครสมาชิกซ้ำเบอร์/อีเมล ได้ "Internal server error" (500) แทนข้อความบอกว่าซ้ำ
- ปัญหา: AuthService.register() ไม่ catch Prisma unique constraint error เลย พอ email หรือ phone ซ้ำกับ user เดิม throw PrismaClientKnownRequestError (P2002) ดิบๆ ขึ้นไป Nest แปลงเป็น 500 generic "Internal server error" ผู้ใช้ไม่รู้เลยว่าปัญหาคืออะไร (เจอจริงจาก user สมัครด้วยเบอร์ 0859295863 ที่มีบัญชีอยู่แล้ว)
- แก้: ครอบ try/catch ใน register(), เช็ค err.code === 'P2002' แยกตาม err.meta.target ว่าเป็น email หรือ phone โยน ConflictException (409) พร้อมข้อความไทยชัดเจน "อีเมลนี้ถูกใช้งานแล้ว" / "เบอร์โทรนี้ถูกใช้งานแล้ว"
- สถานะ: แก้แล้ว, ยืนยันด้วย curl ซ้ำเบอร์เดิมจริง -> คืน 409 {"message":"เบอร์โทรนี้ถูกใช้งานแล้ว"} แทน 500, tsc ผ่าน 0 error

[26] PATCH /notifications/:id/read ไม่เช็คว่า notification เป็นของ user ที่ล็อกอินอยู่จริง
- ปัญหา: NotificationsController.markRead(id) รับแค่ id จาก param ส่งตรงเข้า service.markRead(id) โดยไม่เช็คว่า notification.userId ตรงกับ user ที่ authenticate อยู่เลย ใครก็ตามที่ login อยู่ (role ไหนก็ได้) เดา/รู้ id ของ notification คนอื่น สามารถยิง PATCH ไปมาร์คว่าอ่านแล้วแทนเจ้าของจริงได้ (severity ต่ำ - แค่เปลี่ยน readAt ไม่ใช่ข้อมูลรั่ว แต่เป็นช่องโหว่ authorization จริง)
- พบระหว่าง: สร้างฟีเจอร์ใบตักเตือน (admin ส่ง notification ให้ user) แล้วเพิ่ม UI mark-read บนหน้า /notifications ที่ก่อนหน้านี้ไม่เคยต่อปุ่มไว้เลย
- สถานะ: ยังไม่แก้ (นอกขอบเขตงานที่ขอตอนนี้) - ต้องเพิ่มเช็ค ownership ใน markRead ก่อน update ถ้าจะแก้

[27] MailService.send() ไม่ catch error จาก nodemailer เลย ทำ endpoint ส่งใบตักเตือน 500 ทั้งที่ notification สร้างสำเร็จแล้ว
- ปัญหา: transporter.sendMail() throw ตรงๆ ถ้า SMTP provider reject (เช่น Resend sandbox mode ปฏิเสธส่งไปโดเมนแปลกที่ไม่ verify - "550 Invalid `to` field... use testing email address") ทำให้ทั้ง request 500 ทั้งที่ NotificationsService.create() ทำสำเร็จไปแล้วก่อนหน้า (แค่ยิง email ต่อพัง)
- แก้: ครอบ try/catch ใน MailService.send() log error แล้ว return false แทน throw - endpoint คืน {notified:true, emailSent:false} เสมอไม่ว่า SMTP จะสำเร็จหรือพัง
- สถานะ: แก้แล้ว, ยืนยันด้วย curl คืน {"notified":true,"emailSent":false} แทน 500, tsc ผ่าน 0 error
- หมายเหตุ: SMTP เชื่อมต่อ Resend สำเร็จจริง (ไม่ใช่ปัญหา config) แค่ sandbox ของ Resend (ยังไม่ verify domain) ปฏิเสธส่งไปโดเมนทดสอบอย่าง example.com - ต้องทดสอบด้วยอีเมลจริงของเจ้าของบัญชี Resend เอง หรือ verify domain ก่อนถึงส่งไปอีเมลอื่นได้

[28] SMTP - Resend ยังไม่ verify domain ส่งอีเมลจริงไปหาผู้ใช้ทั่วไปไม่ได้ (ยังไม่ใช่บัคโค้ด แค่ setup ค้าง)
- สถานะ: SMTP เชื่อมต่อ+auth สำเร็จแล้ว (smtp.resend.com, API key ใส่ใน apps/api/.env แล้ว) โค้ด MailService ทำงานถูกต้อง
- ติดอยู่ที่: บัญชี Resend เป็น sandbox mode (ยังไม่ verify โดเมนตัวเอง) ส่งได้แค่ไปอีเมลเจ้าของบัญชี Resend เอง ส่งไปโดเมนอื่น (user ทั่วไป) โดน reject 550
- ต้องทำต่อ (ทีหลัง): ไป resend.com/domains verify โดเมนจริงของ Hopak (ต้องมี domain ของตัวเองก่อน + ตั้ง DNS record) ถึงจะส่งอีเมลใบตักเตือนไปหา user จริงในระบบได้ครบ

[29] สลับภาษา TH/EN ไม่ real-time — ต้อง reload/เปลี่ยนหน้าถึงจะเห็นผล
--------------------------------------------------------
ไฟล์: apps/web/src/hooks/useLang.ts
อาการ: useLang() ใช้ useState ในตัวมันเอง แยกกันคนละ instance ต่อ component
ที่เรียกใช้ (Navbar, layout, แต่ละหน้า) กดสวิตช์ใน Navbar เรียก setLang ของ
instance ตัวเอง อัปเดตแค่ state ตัวเอง + localStorage แต่ instance อื่น (เช่น
เนื้อหาในหน้า) ไม่รู้ว่าเปลี่ยน เพราะ useEffect อ่าน localStorage แค่ตอน mount
ครั้งเดียว ต้อง reload หรือเปลี่ยนหน้า (remount) ถึงจะเห็นภาษาใหม่
ผล: กดสวิตช์ภาษาแล้วเหมือนไม่ทำงาน (จริงๆ ทำงานแค่ปุ่ม ไม่ทำงานที่เนื้อหาหน้า)
แก้ไข: เพิ่ม module-level pub-sub ใน useLang.ts (currentLang + listeners Set)
setLang() อัปเดต localStorage แล้ว broadcast ให้ทุก instance ที่ subscribe อยู่
เปลี่ยน state ทันที ไม่ต้อง reload/remount
สถานะ: แก้แล้ว, tsc --noEmit ผ่าน 0 error (22/07/2026)

[31] Google login callback ส่ง JSON ดิบกลับเบราว์เซอร์แทนที่จะพาเข้าเว็บจริง (เชื่อมต่อจริงไม่ได้)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/auth/auth.controller.ts, google.strategy.ts,
apps/web/src/app/(auth)/login/page.tsx, register/page.tsx
อาการ: GET /auth/google/callback (@UseGuards(AuthGuard('google'))) return
{accessToken, user} ตรงๆ เป็น response ของหน้าเว็บที่เบราว์เซอร์ navigate ไปจริง
(ไม่ใช่ fetch จาก JS) ผู้ใช้เห็นแค่ JSON ดิบเต็มจอ ไม่มีการ setToken/login จริง
ในเว็บเลย + callbackURL ใน GoogleStrategy เป็น path relative ('/auth/google/callback')
ซึ่ง passport-google-oauth20 ต้องการ absolute URL ให้ตรงกับที่ตั้งไว้ใน Google Cloud
Console เป๊ะ ถ้าไม่ตรง Google จะปฏิเสธทันที (redirect_uri_mismatch)
แก้ไข:
- callbackURL เปลี่ยนเป็น absolute ผ่าน env API_URL (${API_URL}/auth/google/callback)
- googleAuthCallback() เปลี่ยนเป็น redirect (res.redirect) ไปหน้าเว็บจริงที่
  ${FRONTEND_URL}/auth/google/callback?token=... (สำเร็จ) หรือ /login?error=...
  (ล้มเหลว เช่น account suspended) แทนการ return JSON ตรงๆ
- เพิ่มหน้าเว็บ apps/web/src/app/(auth)/auth/google/callback/page.tsx อ่าน token
  จาก query, setToken() แล้ว redirect เข้า / (หรือ /login?error=... ถ้าไม่มี token)
- login page เพิ่มอ่าน ?error= จาก query โชว์เป็น error message ให้ผู้ใช้เห็น
- เพิ่ม env FRONTEND_URL, API_URL ใน apps/api/.env
สถานะ: แก้โค้ดแล้ว (เชื่อมต่อจริงได้ทันทีที่ใส่ GOOGLE_CLIENT_ID/SECRET จริง)
ยังต้องทำต่อ (นอกเหนือจากโค้ด): ผู้ใช้ต้องไปสร้าง OAuth Client ID จริงที่
Google Cloud Console เอง (ต้องใช้บัญชี Google ของเจ้าของระบบ) แล้วใส่ค่าใน .env
- GOOGLE_CLIENT_ID / GOOGLE_CLIENT_SECRET (dev + prod .env คนละไฟล์)
- Authorized redirect URI ใน Google Console ต้องตรงกับ API_URL/auth/google/callback
  เป๊ะทุกตัวอักษร (เช่น http://203.170.190.188:4000/auth/google/callback ตอนนี้,
  หรือ https://api.hoprak.com/auth/google/callback หลัง DNS ใช้งานได้)
tsc --noEmit ผ่าน 0 error ทั้ง apps/api และ apps/web (23/07/2026)

[30] แผนที่ (MapPicker) พัง "Error: Map container is already initialized" ตอนกลับมาหน้าเดิม
--------------------------------------------------------
ไฟล์: apps/web/src/components/map/MapPicker.tsx
อาการ: หน้า dorms/[id] และ partner/dorms/new ใช้ react-leaflet's <MapContainer>
Next.js App Router บางครั้ง resume หน้าที่ cache ไว้ (เช่นกด back) โดยไม่ unmount
component tree จริงๆ ก่อน (React "reappearLayoutEffects") ทำให้ layout effect
ของ MapContainer รันซ้ำบน DOM node เดิมที่ยังมีค่า _leaflet_id จากรอบก่อนติดอยู่
Leaflet เช็คเจอค่านี้แล้ว throw ทันที ("Map container is already initialized")
พังทั้งหน้าเป็น Unhandled Runtime Error
แก้ไข: เขียน MapPicker.tsx ใหม่ไม่ใช้ <MapContainer>/<TileLayer>/<Marker> ของ
react-leaflet แล้ว ใช้ Leaflet API ตรง (L.map, L.tileLayer, L.marker) ใน useEffect
เอง พร้อม guard ลบ _leaflet_id เก่าทิ้งก่อนสร้าง map ใหม่ทุกครั้ง (กันกรณี node
ถูก reuse โดยไม่ cleanup) + cleanup จริงเรียก map.remove() ตอน unmount
สถานะ: แก้แล้ว, tsc --noEmit ผ่าน 0 error (22/07/2026)

[32] อัปโหลดรูป/เอกสารหอพักในหน้า partner-register ได้ "Internal server error" (S3 ไม่ได้ตั้งค่า)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/uploads/uploads.service.ts
อาการ: POST /owner-applications/:id/photos และ /:id/documents (ขั้นตอนอัปโหลด
รูปหน้าปก/เอกสารยืนยันในหน้า partner-register wizard) คืน 500 ทุกครั้ง
สาเหตุ: apps/api/.env มี S3_BUCKET/S3_ACCESS_KEY/S3_SECRET_KEY ว่างเปล่าทั้งหมด
(ไม่เคยตั้งค่า S3 จริงในโปรเจกต์นี้เลย) แต่ UploadsService ทำ `new S3Client({})`
ตรงๆ ไม่เช็คก่อน AWS SDK เลย throw "Error: Region is missing" ดิบๆ ขึ้นไป
กลายเป็น 500 generic ไม่บอกสาเหตุจริง
แก้ไข: UploadsService เช็คก่อนว่า S3_BUCKET+S3_ACCESS_KEY+S3_SECRET_KEY ครบ
ทั้ง 3 ตัวมั้ย ถ้าครบค่อยสร้าง S3Client จริง (ใส่ region/credentials ชัดเจน
แทนที่จะปล่อยว่างเหมือนเดิม) ถ้ายังไม่ตั้งค่า fallback เขียนไฟล์ลงดิสก์เครื่อง
ที่ apps/api/uploads/ แทน แล้วคืน URL ที่ served อยู่แล้วผ่าน
useStaticAssets(join(process.cwd(),'uploads'),{prefix:'/uploads/'}) ใน
main.ts (เปิดไว้อยู่แล้วแต่ไม่เคยมีใครเขียนไฟล์ลงไปจริงจนตอนนี้)
สถานะ: แก้แล้ว, ยืนยันด้วย curl: POST photos + documents คืน 200 พร้อม
http://localhost:4000/uploads/... ที่โหลดได้จริง (200) ทั้งคู่
หมายเหตุ: local-disk fallback ใช้ได้แค่ dev (ไฟล์ไม่ถูก commit, ไม่ persist
ข้าม deploy) - ก่อนขึ้น production ต้องตั้งค่า S3/R2/B2 จริงใส่ .env

[32b] ตามมาจาก [32]: เอกสารยืนยันตัวตน (บัตรประชาชน/โฉนด/ทะเบียนบ้าน) เดิม public URL ถาวร ไม่มี auth
--------------------------------------------------------
ไฟล์: apps/api/src/modules/uploads/uploads.service.ts, files.controller.ts (ใหม่),
apps/api/src/common/file-signing.util.ts (ใหม่),
apps/api/src/modules/owner-applications/owner-applications.service.ts,
apps/api/src/modules/admin/approvals/approvals.service.ts
อาการ: หลังแก้ [32] เอกสารอัปโหลดได้แล้วก็จริง แต่ URL ที่คืนกลับ (ทั้ง S3 จริง
และ local fallback) เป็น URL ถาวรสาธารณะ ใครก็เปิดดูได้ถ้ารู้/เดา URL ได้ ไม่มี
การเช็คสิทธิ์เลย - อันตรายเพราะเอกสารพวกนี้มีข้อมูลอ่อนไหว (เลขบัตรประชาชน ที่อยู่
เจ้าของกรรมสิทธิ์) ตาม PDPA ต้องจำกัดการเข้าถึง
แก้ไข: แยก visibility 'public' (รูปหอพัก - ต้องโชว์หน้าค้นหาให้ผู้เช่าเห็น ปล่อย
เป็น URL ถาวรเหมือนเดิม) กับ 'private' (เอกสารยืนยันตัวตน):
- private-uploads/ (แยกจาก uploads/ ที่ served แบบ static) เก็บไฟล์ private,
  ไม่ผูกกับ useStaticAssets เลย เข้าไม่ได้ตรงๆ ผ่าน URL
- DB (OwnerApplication.documents, Dorm.documents) เก็บเป็น storage key ไม่ใช่ URL
- เพิ่ม signFileKey/verifyFileToken (HMAC-SHA256, timingSafeEqual กันจับเวลา)
  สร้าง token หมดอายุ 15 นาที ผูก key+expiry+signature เข้าด้วยกัน
- เพิ่ม GET /files/:token (FilesController) verify token แล้วค่อย stream ไฟล์
  กลับ ไม่ผ่าน token ที่ถูกต้อง/ยังไม่หมดอายุ = 404 ทันที
- ทุกจุดที่ return documents ให้ frontend (owner-applications ทุก endpoint,
  admin/approvals listPending) แปลง key เป็นลิงก์ชั่วคราวใหม่ทุกครั้งที่เรียก
  ไม่ cache/เก็บ URL ถาวรที่ไหนเลย
สถานะ: แก้แล้ว, ยืนยันด้วย curl: token จริงเปิดได้ (200, content-type ถูกต้อง),
token ปลอม/ต่อท้ายผิด = 404, เดา path เดิมใต้ /uploads/ ตรงๆ = 404 (ไม่มีไฟล์
private หลุดออกไปทาง static route เก่าอีกแล้ว) tsc --noEmit ผ่าน 0 error
หมายเหตุ: ถ้าใช้ S3 จริง (bucket ตั้ง private/ prefix) ก็ยังผ่าน token endpoint
เดียวกัน (stream ผ่าน server แทนที่จะ presigned URL ตรง S3 - เพื่อความสม่ำเสมอ
ของโค้ด ถ้าต้องการ scale จริงค่อยเปลี่ยนไปใช้ S3 presigned URL ทีหลังได้)

[33] หน้า admin/finance และหน้าใหม่ที่เพิ่งเพิ่มพัง "Unhandled Runtime Error: Forbidden resource" — apiClient.get ไม่มี .catch()
--------------------------------------------------------
ไฟล์: apps/web/src/app/(admin)/admin/finance/page.tsx,
apps/web/src/app/(admin)/admin/finance/owners/[ownerId]/page.tsx,
apps/web/src/app/(admin)/admin/dashboard/page.tsx,
apps/web/src/app/(admin)/admin/bookings/page.tsx,
apps/web/src/app/(partner)/partner/settings/page.tsx,
apps/web/src/app/(partner)/partner/dashboard/page.tsx
อาการ: user เจอ "Unhandled Runtime Error: Forbidden resource" มาจาก
src/lib/api-client.ts request() throw Error ตรงๆ เวลา response ไม่ ok
(401/403/400 ฯลฯ) — หน้า/component ที่เรียก apiClient.get(...).then(setState)
โดยไม่มี .catch() เลย พอ request พัง (เช่น token ผิด role, session หมดอายุ,
network hiccup) กลายเป็น unhandled promise rejection ทำ Next dev โชว์ error
overlay เต็มจอแทนที่จะ degrade แบบเงียบๆ — บั๊กรูปแบบเดียวกับที่เคยแก้ไปแล้วใน
bugs.txt [24] (หน้าฝั่ง tenant) แต่รอบนี้เกิดซ้ำในหน้าใหม่ที่เพิ่งเขียนเพิ่ม
(admin/finance ระบบ payout, admin/dashboard, admin/bookings cancel/restore,
partner/settings บันทึกบัญชีธนาคาร, partner/dashboard) เพราะเขียนไล่เลี่ยกัน
หลายไฟล์ในรอบเดียวแล้วลืมใส่ .catch() ให้ครบทุกจุดเหมือน pattern เดิมที่เคยแก้
แก้ไข: เพิ่ม .catch() ทุก apiClient.get/patch call ที่ขาด (ตั้ง state fallback
เป็น [] หรือ noop ตามความเหมาะสมของแต่ละหน้า) รวมถึง handleCancel/handleRestore
ใน admin/bookings ที่มี await ไม่มี catch (ห่อ try/catch เงียบไว้ ไม่ให้พังทั้งหน้า)
สถานะ: แก้แล้ว, tsc --noEmit ผ่าน 0 error ทั้ง apps/web และ apps/api
บทเรียน: ทุกครั้งที่เขียนหน้าใหม่ที่ยิง apiClient.get/post/patch ต้องเช็ค .catch()
ให้ครบทันทีตอนเขียน ไม่ใช่รอ user เจอ error ก่อนแล้วค่อยไล่แก้ทีหลัง

ตามมา: user เจอ error เดิมซ้ำอีกครั้งหลังแก้รอบแรก — ไล่เช็คเพิ่มแล้วพบว่าบั๊กนี้
เป็น pattern เดิมที่มีอยู่ทั่วทั้งฝั่ง admin/partner อยู่แล้วก่อนหน้านี้ (ไม่ใช่แค่
ไฟล์ที่เพิ่งเขียนใหม่รอบนี้) ไฟล์เพิ่มเติมที่แก้ในรอบตามมา:
apps/web/src/app/(admin)/admin/approvals/page.tsx (reload + approve/reject),
apps/web/src/app/(admin)/admin/owner-requests/page.tsx (reload + approve/reject),
apps/web/src/app/(admin)/admin/admins/page.tsx (reload),
apps/web/src/app/(admin)/admin/users/page.tsx (toggleSuspend, submitWarning),
apps/web/src/app/(partner)/partner/requests/page.tsx (reload + confirm/reject),
apps/web/src/app/(partner)/partner/rooms/page.tsx (reload + toggleStatus),
apps/web/src/app/(partner)/partner/slips/page.tsx (reload)
เช็คแล้วว่า admin/campaigns, admin/website, admin/dorms/new มี catch/try-catch
อยู่แล้วตั้งแต่ต้น ไม่ต้องแก้เพิ่ม
สถานะ: แก้ครบทั้งหมดแล้ว, tsc --noEmit ผ่าน 0 error ทั้งสอง apps (26/07/2026)

[34] Sidebar admin/partner: มองไม่เห็นปุ่ม logout กับสลับภาษา ต้องเลื่อนจอลงไปหา
--------------------------------------------------------
ไฟล์: apps/web/src/app/(admin)/admin/layout.tsx, apps/web/src/app/(partner)/partner/layout.tsx
อาการ: container นอกสุดใช้ `min-h-screen` (flex row) — พอเนื้อหาในหน้า main
(เช่น partner/dashboard ที่มี KPI+กราฟ+donut+ตาราง เยอะ) สูงเกิน 1 หน้าจอ
ทั้ง container เลยถูกดันให้สูงตาม แล้ว `<aside>` (sidebar) ที่เป็น flex child
ปกติ align-items:stretch จะยืดตามความสูง container ด้วย — sidebar เลยสูงเกิน
จอไปด้วย ผลคือปุ่ม "ออกจากระบบ" กับตัวสลับภาษาที่อยู่ท้ายสุดของ sidebar (หลัง nav
ที่มี flex-1) โดนดันลงไปไกลเกินขอบจอ ต้องเลื่อน scroll ทั้งหน้าลงไปถึงจะเห็น
(ทั้งที่โค้ด render ปุ่มไว้ปกติ ไม่ได้หายไปจริง — เป็นบั๊ก CSS ไม่ใช่ logic)
แก้ไข: เปลี่ยน container นอกสุดจาก `min-h-screen` เป็น `h-screen overflow-hidden`
(fixed เท่าจอเป๊ะ ไม่ยืดตามเนื้อหา) ฝั่ง `<main>` มี `overflow-auto` อยู่แล้ว
เลยสกรอลตัวเองอิสระได้โดยไม่กระทบความสูง sidebar — sidebar คงที่เท่าจอเสมอ
ปุ่ม logout/lang switch เห็นตลอดไม่ต้องเลื่อน (nav เองมี overflow-y-auto อยู่แล้ว
รองรับกรณีเมนูยาวเกินจนต้อง scroll เฉพาะส่วน nav แทน)
สถานะ: แก้แล้วทั้ง 2 layout (เจอ pattern เดียวกันทั้งคู่ แก้พร้อมกัน แม้ user จะ
รายงานแค่ฝั่ง partner) tsc --noEmit ผ่าน 0 error (26/07/2026)

[35] Admin approvals/dashboard ไม่มี realtime, ไม่มี audit trail, ปฏิเสธห้อง = ลบทิ้งถาวรไม่เหลือร่องรอย
--------------------------------------------------------
ไฟล์: apps/api/src/modules/realtime/realtime.gateway.ts, apps/api/src/modules/admin/approvals/approvals.service.ts,
apps/api/src/modules/dorms/dorms.service.ts, apps/api/src/modules/rooms/rooms.service.ts,
apps/web/src/app/(admin)/admin/approvals/page.tsx, apps/web/src/app/(admin)/admin/dashboard/page.tsx
อาการ: (1) หน้า /admin/approvals และ dashboard ดึงข้อมูลครั้งเดียวตอน mount ไม่มี
polling/websocket เลย — เจ้าของหอส่งหอ/ห้องใหม่มา แอดมินไม่เห็นจนกว่าจะรีเฟรชเอง
(2) ไม่มี table เก็บประวัติการอนุมัติ/ปฏิเสธเลยในระบบ ปฏิเสธห้อง = `prisma.room.delete`
ตรงๆ ไม่เหลือร่องรอยอะไรให้ตรวจสอบย้อนหลังได้ (3) ปุ่ม "อนุมัติ" บนหน้า dashboard
เป็นแค่ `<Link href="/admin/approvals">` — กดแล้วไม่อนุมัติจริง แค่พาไปหน้าเดียวกับ
ปุ่ม "ดูเอกสาร" (สองปุ่มพาไปที่เดียวกันเป๊ะ)
แก้ไข: เพิ่ม `RealtimeGateway.emitToRole()` (JWT มี role อยู่แล้วแต่เดิมไม่เคยใช้
จอย room ตาม role เลย) ให้ dorm/room ใหม่ broadcast ไปห้อง `role:admin` แบบ
เรียลไทม์ผ่าน socket.io, เพิ่ม Prisma model `ApprovalLog` เก็บทุก action
(APPROVED/REJECTED/EDITED/SUSPENDED/UNSUSPENDED) พร้อม snapshot เต็มก่อนลบ
ห้องที่ถูกปฏิเสธ, แก้ปุ่มปลอมบน dashboard ให้เรียก API approve จริง
สถานะ: แก้แล้ว + ทดสอบผ่าน curl ครบ (สร้างหอทดสอบ→approve→suspend→เช็คหาย
จากหน้าค้นหา→unsuspend→เช็คกลับมา→reject ห้อง→เช็ค log มี snapshot ก่อนถูกลบ)
tsc --noEmit ผ่าน 0 error ทั้ง 2 app (26/07/2026)

[36] GET /dorms/:dormId/rooms รั่วห้องที่ยังไม่อนุมัติออกสู่สาธารณะ
--------------------------------------------------------
ไฟล์: apps/api/src/modules/rooms/rooms.service.ts (listByDorm)
อาการ: endpoint นี้เปิดสาธารณะ ไม่มี guard และไม่กรอง `approved: true` เลย —
ใครก็ยิง GET ตรงๆ เห็นห้องที่ยังไม่ผ่านตรวจ (approved:false) ได้หมด แม้หน้า
ค้นหาจริงจะไม่ได้เรียก endpoint นี้ (ใช้ dorm.rooms ที่กรองแล้วจาก GET /dorms/:id
แทน) แต่ endpoint เองยังเป็นช่องโหว่ที่เรียกตรงได้
แก้ไข: เพิ่ม `approved: true` ใน where clause
สถานะ: แก้แล้ว + ทดสอบผ่าน curl ยืนยันห้อง approved:false ไม่โผล่ใน endpoint นี้แล้ว
(26/07/2026)

[37] หน้าค้นหา/หน้ารายละเอียดหอพัก (ฝั่งผู้เช่า) ไม่โชว์รูปจริงเลย — render placeholder text แทนตลอด
--------------------------------------------------------
ไฟล์: apps/web/src/app/(tenant)/search/page.tsx, apps/web/src/app/(tenant)/dorms/[id]/page.tsx
อาการ: การ์ดผลลัพธ์ค้นหา (ทั้ง sponsored และปกติ) กับ gallery หน้ารายละเอียดหอพัก
render ข้อความ placeholder ("ไม่มีรูปหอพัก"/"รูปหลัก" ฯลฯ) ตรงๆ ไม่เคยอ้างอิง
`dorm.images` เลยแม้แต่ตัวเดียว — เป็นสาเหตุจริงที่ผู้ใช้รายงานว่า "รูปที่อัปโหลด
เข้ามาไม่ขึ้นเลย" (ไม่ใช่บั๊ก upload/URL — หน้า admin/partner ใช้ URL เดียวกัน
render `<img>` ได้ปกติอยู่แล้ว เป็นแค่หน้าฝั่งผู้เช่าลืมใส่ `<img>` เข้าไปจริงๆ)
แก้ไข: เพิ่ม `<img src={dorm.images[n]}>` จริงในทั้ง 2 หน้า โดย fallback เป็น
placeholder เดิมเฉพาะตอนไม่มีรูป
สถานะ: แก้แล้ว tsc --noEmit ผ่าน 0 error, ทดสอบเปิดหน้าจริงผ่าน dev server
คอมไพล์สำเร็จไม่มี error (26/07/2026)

[38] Admin console: ค้นหา/กระดิ่งแจ้งเตือน/ปุ่มเพิ่มผู้ใช้ เป็นแค่ UI ตกแต่ง ไม่ทำงานจริงสักอย่าง
--------------------------------------------------------
ไฟล์: apps/web/src/app/(admin)/admin/layout.tsx, apps/web/src/app/(admin)/admin/users/page.tsx
อาการ: ช่องค้นหาที่ header เป็น `<div>` ธรรมดา ไม่ใช่ `<input>` เลย (มี
title="เร็วๆ นี้"/"Coming soon" ยืนยันว่าตั้งใจไม่ทำ), กระดิ่งเป็นแค่ icon
คงที่ ไม่มี onClick, ปุ่ม "+เพิ่มผู้ใช้" เป็น `disabled` hardcode พร้อม tooltip
บอกตรงๆ ว่ายังไม่เปิดใช้งาน — และฝั่ง backend ไม่มี `POST /admin/users`
เลยด้วย (สร้างผู้ใช้ทำได้แค่ผ่านหน้าสมัครสมาชิกจริง)
แก้ไข: เพิ่ม `POST /admin/users` (แอดมินสร้างบัญชีตรงได้ ไม่ต้องรอสมัคร/
อนุมัติ), เพิ่ม `GET /admin/users/:id/documents` (ดูเอกสารเจ้าของหอกันทุจริต),
ทำช่องค้นหาจริง (debounce 300ms, ค้นหอ/ผู้ใช้/การจอง), ทำกระดิ่งเป็น dropdown
จริงแสดงรายการรออนุมัติ + อัปเดตเรียลไทม์ผ่าน socket
สถานะ: แก้แล้ว + ทดสอบผ่าน curl (สร้าง user จริงผ่าน endpoint ใหม่, ค้นหาเจอ
หอ/user/booking จริง) tsc --noEmit ผ่าน 0 error (26/07/2026)

[แก้แล้ว] [39] Production: DNS ใช้งานได้แล้ว แต่ nginx บน server จริงไม่แยก server_name + HTTPS ยังไม่พร้อม
--------------------------------------------------------
ไฟล์: docker/nginx-hopak.conf (ไฟล์ในโปรเจกต์ vs config จริงบน server ไม่ตรงกัน)
อาการ: เช็ค DNS ผ่านหมดแล้ว — NS ชี้ th13/th14.hostatom.com ถูกต้อง, A record
hoprak.com/www.hoprak.com/app.hoprak.com/api.hoprak.com ทุกตัวชี้ 203.170.190.188
ถูกต้องหมด, HTTP (port 80) ตอบ 200 จริง — แต่ทดสอบด้วย curl -H "Host: ..."
ตรงไป IP พบว่า hoprak.com, app.hoprak.com, api.hoprak.com ทั้ง 3 โดเมนตอบ
เนื้อหาเดียวกันเป๊ะ (หน้า Next.js frontend) — api.hoprak.com ควรเป็น backend
NestJS (port 4000) แต่กลับเจอหน้าเว็บแทน แปลว่า nginx บน server จริงไม่ได้แยก
server_name ตาม docker/nginx-hopak.conf ที่ทำไว้ในโปรเจกต์ (ไฟล์นั้นตั้งให้
hoprak.com redirect 301 ไป app.hoprak.com, api.hoprak.com proxy ไป :4000 —
แต่พฤติกรรมจริงไม่ตรงเลย ไม่มี redirect ด้วยซ้ำ) น่าจะเป็นเพราะ config บน
server จริงยังเป็นค่า default (proxy ทุกอย่างไป :3001) ไม่ได้ apply ไฟล์นี้
เพิ่มเติม: port 443 (HTTPS) closed/filtered จาก server — ยังไม่ได้ตั้ง SSL
(certbot) เลย เข้า https://hoprak.com ไม่ได้เลยตอนนี้
สถานะ: ยังไม่แก้ (ต้องทำบน server จริงผ่าน SSH ไม่ใช่แก้โค้ดในโปรเจกต์) —
แก้ไข: รื้อของเก่าทั้งหมดบน VPS ทิ้ง (container+volume+nginx site+cert เก่า)
แล้ว deploy ใหม่ทั้งชุด (ดู DEPLOY.md ขั้นตอน 0-11): clone โค้ดใหม่, copy
docker/nginx-hopak.conf เข้า sites-available/sites-enabled จริง, รัน
certbot --nginx -d app.hoprak.com -d api.hoprak.com -d hoprak.com -d www.hoprak.com
สำเร็จ ออก cert จริง — แต่ HTTPS ยังเข้าไม่ได้อีกจุด: `ufw status` เจอ firewall
เปิดแค่ port 22/80 ("Nginx HTTP" profile เท่านั้น) ไม่เคยเปิด 443 เลยตั้งแต่ต้น
(สาเหตุที่แท้จริงที่ทำให้ port 443 closed/filtered จากภายนอกทั้งที่ nginx/cert
พร้อมแล้ว) แก้ด้วย `ufw allow 443/tcp`
ระหว่างทางเจอเพิ่ม (บั๊กใหม่ ไม่เกี่ยวกับ DNS/nginx โดยตรง): production Docker
build ของ apps/api ล่ม restart loop ตลอด เพราะ `multer` เป็นแค่ transitive
dependency ผ่าน @nestjs/platform-express ไม่เคยประกาศตรงใน
apps/api/package.json (มีแต่ @types/multer) — ใช้งานได้ตอน dev local เพราะ
pnpm hoist ให้ แต่ Docker build (strict node_modules) หาไม่เจอ = MODULE_NOT_FOUND
แก้เพิ่ม dependency ตรง "multer": "2.0.2" ใน package.json + อัปเดต
pnpm-lock.yaml, และ Dockerfile.api runner stage เดิมไม่ copy
apps/api/package.json + tsconfig.json เข้าไปเลย ทำให้ `npm run db:seed`/
`npx prisma migrate deploy` (ที่รันหลัง deploy) หา script ไม่เจอ (ENOENT
package.json) และ npx ไปดึง prisma เวอร์ชันล่าสุดที่ไม่ตรงกับที่ pin ไว้แทน —
แก้เพิ่ม COPY 2 ไฟล์นี้เข้า runner stage ด้วย
สถานะ: แก้แล้วทั้งหมด — ทดสอบจริงจากภายนอก (curl -I) ยืนยัน:
https://app.hoprak.com → 200 Next.js, https://api.hoprak.com → 404
Express/NestJS (ถูกต้อง คนละ backend จริงแล้ว), https://hoprak.com → 301
ไป app ถูกต้อง, cert หมดอายุ 2026-10-25 auto-renew ตั้งไว้แล้ว (27/07/2026)
ยังต้องทำต่อ (นอกเหนือจากโค้ด/infra): อัปเดต Google OAuth Authorized redirect
URI เป็น https://api.hoprak.com/auth/google/callback ใน Google Cloud Console
(เกี่ยวโยงกับ [31]), เปลี่ยนรหัสผ่าน admin/owner จาก seed default "hopak1234"
ทันทีหลัง login ครั้งแรกบน production

[41] หน้า Profile: ยืนยันตัวตนด้วย OTP ทางอีเมล — error message มองไม่เห็นถ้าส่งพลาดตอนยังไม่เปิดกล่องกรอกรหัส
--------------------------------------------------------
ไฟล์: apps/web/src/app/(tenant)/profile/page.tsx (VerifySection)
อาการ: ระหว่างทดสอบ endpoint ใหม่ POST /users/me/send-verification-otp (เพิ่ง
เพิ่มฟีเจอร์ยืนยันอีเมลด้วย OTP จริง ใช้ MailService/Resend เดิม) เจอว่าถ้า
ส่งรหัสไม่สำเร็จ (เช่น ชนช่วง cooldown 60 วิ ขอรหัสใหม่ถี่เกินไป) โค้ด setError()
ไว้ก็จริง แต่ <p>{error}</p> อยู่ใน JSX block ที่ render เฉพาะตอน
stage==='input'||'verifying' เท่านั้น — พอส่งพลาดตั้งแต่ตอน stage ยังเป็น 'idle'
(กลับไป idle ใน catch) ข้อความ error เลยไม่ถูก render เลย ผู้ใช้กดปุ่ม "ส่งรหัส
OTP" แล้วเหมือนไม่มีอะไรเกิดขึ้น ไม่รู้สาเหตุ
แก้ไข: ย้าย error paragraph ออกมาเป็นเงื่อนไขแยก แสดงตอน stage==='idle'||'sending'
ด้วย (เพิ่มจากเดิมที่โชว์เฉพาะ input/verifying)
สถานะ: แก้แล้ว, ทดสอบ flow เต็ม (ส่งรหัส→อ่านโค้ดจาก log dev-mode→กรอก→ยืนยัน
สำเร็จ emailVerified:true, badge/tag ขึ้นถูกต้อง) ผ่าน curl+browser จริง
tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (27/07/2026)
หมายเหตุ: เพิ่ม User.emailVerified/otpCodeHash/otpExpiresAt/otpSentAt/otpAttempts
ใน schema (migration add_user_email_otp) รวมถึงเจอ+แก้ระหว่างทาง: auth.service
omitPassword() เดิมกรองแค่ password ไม่กรอง otpCodeHash — ขยายให้กรองคู่กัน
กันรั่ว hash ของรหัส OTP ออกไปกับ response register/login/google-callback ทุกจุด

[42] partner-register: สมัครซ้ำด้วยอีเมลเดิม (ใบสมัครเก่าค้างไม่เสร็จ) โผล่รูป/เอกสารจากรอบก่อนหน้า
--------------------------------------------------------
ไฟล์: apps/api/src/modules/owner-applications/owner-applications.service.ts (start)
อาการ: user รายงานว่าตอนสมัครเปิดหอพักใหม่ (partner-register step "ข้อมูลหอพัก")
เจอรูปหอพัก/โลโก้ที่ไม่ใช่ของตัวเอง (เช่น รูปหน้าโรงแรม รูปห้อง โลโก้บริษัทรักษา
ความปลอดภัย) ขึ้นมาให้เห็นทั้งที่ยังไม่เคยอัปโหลดอะไรเลยในรอบนี้ (ช่อง Choose
Files ยังโชว์ "No file chosen" ทั้งคู่)
ต้นตอ: OwnerApplicationsService.start() เช็คอีเมลซ้ำกับ OwnerApplication เดิม
ที่ยังไม่ COMPLETED (เช่น ทดสอบสมัครค้างไว้ก่อนหน้าไม่จบ) แล้ว "resume" ใบสมัคร
เดิมนั้นต่อ — update แค่ name/phone แต่ images/documents/otp state เดิมยังติด
มาด้วยทั้งดุ้น ขัดกับ comment ในหน้าเว็บเองที่บอกไว้ชัดว่า "ไม่จำข้อมูลเดิมข้าม
เซสชัน — ออกจากหน้านี้แล้วกลับมาต้องกรอกใหม่ทุกครั้ง"
แก้ไข: ตอน resume ใบสมัครเดิม (อีเมลซ้ำ ยังไม่ COMPLETED) reset images:[],
documents:[], status กลับเป็น DRAFT, ล้าง otpCodeHash/otpExpiresAt/otpSentAt/
otpAttempts/verifiedAt ทั้งหมด — เริ่มเก็บรูป/เอกสาร/ยืนยันอีเมลใหม่ทุกครั้งจริง
สถานะ: แก้แล้ว + ทดสอบผ่าน curl จริง (สร้างใบสมัคร→อัปโหลดรูปทดสอบ→สมัครซ้ำ
อีเมลเดิม→ยืนยัน images:[] ว่างจริง) tsc --noEmit ผ่าน 0 error (27/07/2026)

[43] [ร้ายแรง] partner-register: สมัครเจ้าของหอสำเร็จแล้ว role กลายเป็น "ผู้เช่า" ไม่ใช่ "เจ้าของหอ"
--------------------------------------------------------
ไฟล์: apps/api/src/modules/owner-applications/owner-applications.service.ts (finish)
อาการ: user สมัครผ่าน partner-register wizard ครบ 4 ขั้นตอน (กรอกข้อมูลหอพัก+
รูป+เอกสาร+ยืนยันอีเมล OTP+ตั้งรหัสผ่าน) เสร็จสมบูรณ์ แต่หน้า admin/users
โชว์ role เป็น "ผู้เช่า" ไม่ใช่ "เจ้าของหอ" — บัญชีที่สมัครมาเพื่อเปิดหอพักโดยตรง
กลับใช้งานเป็นเจ้าของหอจริงไม่ได้เลย (เมนู/สิทธิ์ฝั่ง owner ทั้งหมดไม่โผล่ เพราะ
role จริงในระบบคือ TENANT)
ต้นตอ: finish() สร้าง User ด้วย `role: 'TENANT'` hardcode ทั้งที่ทั้ง flow นี้คือ
การสมัครเป็นเจ้าของหอพักโดยเฉพาะ (สร้าง Dorm พร้อม ownerId ผูกกับ user นี้ในทรานแซคชัน
เดียวกันเลยด้วยซ้ำ) — น่าจะเป็นการ copy-paste ผิดจากที่อื่น แถมยังสร้าง
`OwnerRequest` (โมเดลสำหรับ flow อื่น — tenant เดิมกดขอเป็นเจ้าของหอในหน้าโปรไฟล์
แบบไม่มีข้อมูลหอ) ซ้ำซ้อนเข้าไปอีก ทำให้ user โผล่ผิดคิวใน admin/owner-requests
ด้วยทั้งที่หอถูกสร้างจริงแล้วและไปรอที่ admin/approvals อยู่แล้ว
แก้ไข: เปลี่ยนเป็น `role: 'OWNER'`, ลบการสร้าง OwnerRequest ซ้ำซ้อนทิ้ง (Dorm
ที่สร้างพร้อมกันมี status PENDING_APPROVAL อยู่แล้ว ผ่าน review จริงที่
admin/approvals ตามปกติ ไม่ต้องพึ่ง OwnerRequest อีกชั้น)
เจอ user จริงที่โดนบั๊กนี้ไปแล้ว 1 คน ("น้อง บีม" เจ้าของหอ "ปิยดิส เพลส")
แก้ role + ลบ OwnerRequest ค้างให้แล้วบน local DB
สถานะ: แก้โค้ดแล้ว (local เท่านั้น) tsc --noEmit ผ่าน 0 error — ยังไม่ commit/push/
deploy ขึ้น production ตามคำสั่ง user ("อย่าพึ่งขึ้นโปรดักชั่น")
ค้างทำ: (1) commit+push+deploy โค้ดนี้ขึ้น production เมื่อพร้อม (2) เช็คว่า
"น้อง บีม" สมัครผ่าน production หรือ local — ถ้า production ต้องรัน SQL แก้
role เดียวกันบน production DB ด้วย (ให้ user รันเองผ่าน SSH เพราะเข้า VPS เองไม่ได้):
UPDATE "User" SET role='OWNER' WHERE phone='0942860595';
DELETE FROM "OwnerRequest" WHERE "userId" IN (SELECT id FROM "User" WHERE phone='0942860595');
(3) เช็คด้วยว่ามี user อื่นที่โดนบั๊กเดียวกันบน production อีกไหม — query:
SELECT u.id,u.name,u.role,d.name FROM "User" u JOIN "Dorm" d ON d."ownerId"=u.id
WHERE u.role != 'OWNER';
(27/07/2026)

[44] [ช่องโหว่ทุจริต] ยอดชำระเงินถูก "เคลียร์บิล" อัตโนมัติทุกเที่ยงคืน ไม่มีแอดมินตรวจสลิปเลย
--------------------------------------------------------
ไฟล์: apps/api/src/modules/payments/payments.cron.ts (ลบทิ้งแล้ว), payments.service.ts,
apps/api/src/modules/admin/finance/finance.service.ts, finance.controller.ts,
apps/web/src/app/(admin)/admin/finance/page.tsx
อาการ: payment ทุกตัวที่สถานะ PENDING (ลูกค้าอัปโหลดสลิปจองแล้ว) จะถูก cron
job (@Cron EVERY_DAY_AT_MIDNIGHT) เปลี่ยนเป็น SETTLED "อัตโนมัติ" ทุกเที่ยงคืน
โดยไม่มีแอดมินตรวจสอบสลิป/ยืนยันว่าลูกค้าเข้าหอจริงเลยแม้แต่ขั้นตอนเดียว —
เท่ากับสลิปปลอม/รายการผิดพลาดก็ยังถูกนับเป็น "พร้อมโอนให้เจ้าของหอ" ได้
เจ้าของระบบ (ตัวกลางรับเงิน) ขอให้ตัดระบบอัตโนมัตินี้ทิ้ง ให้แอดมินเป็นคนกด
เคลียร์บิลเองทุกครั้งหลังตรวจสลิป/ยืนยันลูกค้าเข้าหอแล้วเท่านั้น กันข้อผิดพลาด
แก้ไข: ลบ PaymentsCron + @Cron ทิ้งทั้งหมด (ไม่มีระบบอัตโนมัติเปลี่ยนสถานะเงิน
อีกต่อไป), เพิ่ม FinanceService.settlePayment() + endpoint
PATCH /admin/finance/payments/:paymentId/settle ให้แอดมินกดเคลียร์บิลเองทีละ
รายการ (เช็ค status ต้องเป็น PENDING เท่านั้น กันกดซ้ำ), แก้ listDetailed()
เดิม filter เอาแต่ SETTLED/TRANSFERRED (ซ่อน PENDING จากแอดมินไปเลย ไม่มีทาง
เห็นรายการรอเคลียร์) ให้รวม PENDING เข้ามาด้วยพร้อมปุ่ม "เคลียร์บิล" ในตาราง
สถานะ: แก้แล้ว + ทดสอบผ่าน curl จริง (สร้าง payment PENDING→เคลียร์บิล→เช็ค
booking status เปลี่ยนเป็น PAID จริง→กดเคลียร์ซ้ำโดน 400 กันไว้ถูกต้อง) +
เช็คหน้าเว็บจริงเห็นแถว PENDING ขึ้นป้าย "รอเคลียร์บิล" สีแดงพร้อมปุ่มถูกต้อง
tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (27/07/2026)
เกี่ยวโยงกับ [43] ก่อนหน้า (ธีมเดียวกัน: ตัดขั้นตอนอัตโนมัติที่เสี่ยงทุจริต/
ผิดพลาด ให้แอดมินควบคุมด้วยมือแทน ทั้งการเคลียร์บิลและการเลือกโอนทีละหอ)

[45] การเงิน & รวมบิล: โชว์หอทั้งหมด (ไม่ใช่แค่หอมียอดค้าง) + โอนได้ทุกหอแม้ยอด ฿0
--------------------------------------------------------
ไฟล์: apps/api/src/modules/admin/finance/finance.service.ts, finance.controller.ts,
apps/web/src/app/(admin)/admin/finance/page.tsx
อาการ/คำขอ: เดิม "รอโอนเงิน" filter แสดงเฉพาะหอที่มี payment SETTLED ค้างอยู่
เท่านั้น เจ้าของระบบขอให้โชว์หอทั้งหมด (อนุมัติแล้ว) เลือกดูได้ทุกหอ พร้อมกดโอน
เงินได้ทันทีแม้ยอดในระบบเป็น ฿0 (ยืนยันชัดเจนหลังถามกลับ 2 รอบ: "ให้แอดมิน
พิมพ์ยอดเองอิสระ ไม่ผูกกับ payment ใดๆ") — เผื่อกรณีโอนพิเศษ/โบนัส/ปรับยอดเอง
นอกเหนือจากที่ระบบติดตามอัตโนมัติ
แก้ไข:
- listPendingPayouts() เปลี่ยนจาก query เฉพาะ payment SETTELD เป็น query หอ
  APPROVED ทั้งหมดก่อน แล้ว left-join ยอดค้างโอนทีหลัง (หอไม่มียอดค้าง = 0)
- transferForDorm() เอาเงื่อนไข throw ตอน payments.length===0 ออก อนุญาตให้
  admin พิมพ์ยอดเองได้เมื่อไม่มี payment ผูกจริง (บังคับต้องพิมพ์ยอด > 0 เอง
  ถ้าไม่มี payment ให้คำนวณอัตโนมัติ) — ถ้ามี payment SETTLED จริงก็ยัง mark
  TRANSFERRED ให้ตามปกติเหมือนเดิม ไม่กระทบ flow เดิม
- เพิ่ม audit trail ทุกครั้งที่โอน (ไม่ว่าจะผูกกับ payment หรือไม่) บันทึกลง
  ApprovalLog (entityType:'PAYOUT', action:'MANUAL_TRANSFER', adminId, snapshot
  ยอด/หอ/payment ที่เกี่ยวข้อง) กันไม่ให้การโอนแบบ "พิมพ์ยอดเอง" หลุดไปโดยไม่มี
  ร่องรอยเลย (ใช้ table เดิมที่มีอยู่แล้ว ไม่ต้อง migration ใหม่)
- frontend: การ์ดหอโชว์ทุกหอ, ปุ่ม "โอนเงิน" กดได้เสมอไม่ disable, ช่องค้นหา
  หอ/เจ้าของหอเพิ่มมาด้วย (เผื่อหอเยอะขึ้นหลังโชว์ทั้งหมด), modal เตือนเมื่อไม่มี
  payment ผูกอยู่ ("ระบุยอดที่จะโอนเอง")
สถานะ: แก้แล้ว + ทดสอบผ่าน curl จริง (โอนหอที่ไม่มี payment เลยด้วยยอดพิมพ์เอง
500 สำเร็จ, เช็ค ApprovalLog บันทึกถูกต้องครบ) tsc --noEmit ผ่าน 0 error
ทั้ง 2 apps (27/07/2026)

[46] [ร้ายแรง] Login ด้วย Google ล่มทันที ถ้าอีเมลนั้นเคยสมัครด้วย email/password มาก่อน
--------------------------------------------------------
ไฟล์: apps/api/src/modules/auth/auth.service.ts (loginWithGoogle)
อาการ: user เจอ error 500 ดิบๆ ("Unique constraint failed on the fields: (email)")
ตอนกด "เข้าสู่ระบบด้วย Google" ด้วยอีเมลที่เคยสมัครสมาชิกปกติ (email+password)
ไว้ก่อนแล้ว — เข้าระบบไม่ได้เลยทั้งสองทาง (password เดิมก็มี แต่ลองกด Google
กลับพังทันที)
ต้นตอ: loginWithGoogle() หา user ด้วย googleId ก่อน ถ้าไม่เจอ (ปกติเพราะบัญชี
เดิมสมัครด้วย password ไม่เคยมี googleId) จะ `prisma.user.create()` สร้างใหม่
ทันทีโดยไม่เช็คก่อนว่ามี user ใช้อีเมลนี้อยู่แล้วหรือยัง — ชน unique constraint
ที่ email (User.email @unique) พังทันทีเป็น 500
แก้ไข: เพิ่มขั้นตอนเช็คอีเมลก่อน create — ถ้ามี user เดิมอยู่แล้ว (สมัครด้วย
password มาก่อน) ให้ผูก googleId เข้าบัญชีเดิมแทน (update ไม่ใช่ create) ทำให้
login ได้ทั้ง 2 ทาง (password เดิม หรือ Google) เข้าบัญชีเดียวกัน ถ้าไม่เจอทั้ง
googleId และ email ถึงจะสร้างบัญชีใหม่จริงๆ เหมือนเดิม
สถานะ: แก้แล้ว + ทดสอบจำลอง logic จริงผ่าน node script ตรง (บัญชีมี email+
password อยู่ก่อน → login ด้วย Google อีเมลเดียวกัน → ผูกสำเร็จ ไม่ crash,
googleId ถูก set เข้าบัญชีเดิมถูกต้อง) tsc --noEmit ผ่าน 0 error (27/07/2026)

[47] [gap] "ลืมรหัสผ่าน?" หน้า login เป็นลิงก์ตาย ไม่มี flow จริงเลยทั้งระบบ
--------------------------------------------------------
ไฟล์: apps/web/src/app/(auth)/login/page.tsx (บรรทัด ~221)
อาการ: ปุ่ม/ลิงก์ "ลืมรหัสผ่าน?" เป็น `<a href="#">` เฉยๆ กดแล้วไม่ทำอะไร (jump
กลับหัวหน้าเว็บ) — grep ทั้ง apps/web และ apps/api ไม่เจอ route/endpoint สำหรับ
reset password เลยแม้แต่จุดเดียว (ไม่มีหน้า forgot-password, ไม่มี
POST /auth/forgot-password, ไม่มี token ยืนยันการรีเซ็ต ฯลฯ)
ผล: user ที่ลืมรหัสผ่านจริงๆ ไม่มีทางกู้บัญชีได้เองเลย ต้องให้แอดมินช่วยแก้มือ
แก้ไข (user สั่งให้ทำจริงแล้ว 27/07/2026):
- schema: เพิ่ม User.resetTokenHash / resetTokenExpiresAt / resetTokenSentAt
  (migration add_password_reset) เก็บเป็น sha256 ของ token ไม่ใช่ตัว token เอง
  — ตัวจริงอยู่ในลิงก์ที่ส่งไปอีเมลเท่านั้น หลุด DB ไปก็รีเซ็ตรหัสใครไม่ได้
  ใช้ sha256 แทน bcrypt เพราะต้อง lookup ด้วย hash ตรงๆ ตอน user กดลิงก์กลับมา
  (token เป็น randomBytes(32) อยู่แล้ว ไม่มีทาง brute-force เหมือนรหัสที่คนตั้งเอง)
  พร้อม @@index([resetTokenHash]) ให้ lookup ไม่ scan ทั้งตาราง
- POST /auth/forgot-password: คืน { ok: true } เสมอไม่ว่าอีเมลจะมีในระบบหรือไม่
  — กัน user enumeration (ถ้าตอบต่างกันจะกลายเป็นเครื่องมือให้คนไล่เช็คว่า
  อีเมลไหนสมัครไว้แล้วบ้าง) + cooldown 60 วินาทีกันสแปมยิงซ้ำ + token อายุ 30 นาที
- POST /auth/reset-password: เช็ค token/วันหมดอายุ/บัญชีถูกระงับ แล้วตั้งรหัสใหม่
  ล้าง token ทิ้ง (ใช้ได้ครั้งเดียว) และ **revoke ทุก Session ของ user คนนั้น**
  ในทรานแซกชันเดียวกัน — จุดนี้สำคัญ: ถ้าเปลี่ยนแค่รหัสผ่านอย่างเดียว token JWT
  เดิมของคนที่ขโมยบัญชีไปยังใช้ต่อได้อีก 7 วัน (ต่อยอดจาก Session model ของ [49])
- frontend: หน้าใหม่ /forgot-password (กรอกอีเมล + หน้าจอ "ส่งลิงก์แล้ว" ที่ใช้
  ข้อความกลางๆ ไม่บอกว่าอีเมลนี้มีบัญชีจริงหรือไม่) และ /reset-password?token=...
  (รหัสใหม่ + ยืนยันรหัส, เช็คความยาว/ตรงกันก่อนยิง, จัดการเคสไม่มี token ในลิงก์)
  แก้ `<a href="#">` ในหน้า login เป็น <Link href="/forgot-password"> ของจริง
สถานะ: แก้แล้ว + ทดสอบผ่าน curl จริงครบทุกเคส (token ผิด 401, รหัสสั้นไป 400,
รีเซ็ตสำเร็จ 200, ใช้ token ซ้ำโดน 401 ถูกต้อง, รหัสเดิม login ไม่ได้แล้ว, รหัสใหม่
login ได้, token เดิมของ session ที่ค้างอยู่โดน 401 ทันทีหลังรีเซ็ต, cooldown 60
วินาทีทำงานจริง ยิง 2 ครั้งติดได้ลิงก์เดียว, อีเมลผิดฟอร์แมตโดน 400) + ทดสอบ
กดจริงในเบราว์เซอร์ครบทั้ง flow (กรอกอีเมลกดส่ง เห็นหน้า "ส่งลิงก์แล้ว" จริง →
เปิดลิงก์จากอีเมล ตั้งรหัสใหม่ เห็นหน้าสำเร็จจริง → login ด้วยรหัสใหม่ผ่าน)
tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (27/07/2026)

[48] เจ้าของหอ: ดูสลิปโอนเงิน payout ย้อนหลังผ่านหน้าแจ้งเตือนในระบบได้แล้ว (ไม่ใช่แค่ทางอีเมล)
--------------------------------------------------------
ไฟล์: apps/api/prisma/schema.prisma (migration add_notification_attachment),
apps/api/src/modules/notifications/{notifications.service,controller,module}.ts,
apps/api/src/modules/admin/finance/finance.service.ts,
apps/web/src/app/(partner)/partner/notifications/page.tsx
คำขอ: เจ้าของหออยากดูสลิปที่แอดมินโอนเงินให้ย้อนหลังได้ ทั้งทางอีเมลและใน
ระบบ (หน้า /partner/notifications) — เดิมมีแค่ทางอีเมล (แนบไฟล์สลิปมาด้วยตอน
โอนอยู่แล้ว) แต่หน้าแจ้งเตือนในระบบมีแค่ข้อความ ไม่มีทางเห็นรูปสลิปเลย
แก้ไข: เพิ่ม Notification.attachmentKey (เก็บ private storage key ไม่ใช่ URL
ถาวร) — finance.service ส่ง transferSlipKey เข้าไปตอนสร้าง notification แล้ว
เพิ่ม GET /notifications/:id/attachment (เช็ค ownership ก่อนเสมอ) คืน signed
URL ใหม่สดทุกครั้งที่ขอ (ไม่ cache ตายตัว กัน URL หมดอายุ) หน้าเว็บเพิ่มปุ่ม
"🧾 ดูสลิป" ต่อรายการที่มีไฟล์แนบ + โชว์วันที่/เวลาของแต่ละแจ้งเตือน (ประวัติ
ย้อนหลังเดิมมีอยู่แล้วจาก GET /notifications แต่ไม่เคยโชว์เวลาให้เห็นชัด)
ระหว่างทางแก้เพิ่ม: markRead() เดิมไม่เช็ค ownership เลย (บั๊ก [26] ที่เคย
บันทึกไว้ค้างนาน) — เจอโค้ดจุดเดียวกันพอดีเลยแก้ไปด้วย เพิ่มเช็คว่า
notification.userId ตรงกับ user ที่ล็อกอินก่อน mark ทุกครั้ง
สถานะ: แก้แล้ว + ทดสอบผ่าน curl จริงครบ (โอนเงิน→attachmentKey บันทึกถูก→
เจ้าของหอตัวจริงเปิดสลิปได้ 200→user อื่นโดนบล็อก 403 ถูกต้อง) + เช็คหน้าเว็บ
จริงเห็นปุ่มดูสลิปกดได้ tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (27/07/2026)

[49] หน้า Profile: "อุปกรณ์ที่เข้าสู่ระบบ" ทำของจริงแล้ว (ดูประวัติ + เตะออกจากอุปกรณ์ได้จริง)
--------------------------------------------------------
ไฟล์: apps/api/prisma/schema.prisma (migration add_session_tracking),
apps/api/src/modules/auth/{auth.service,auth.controller,strategies/jwt.strategy}.ts,
apps/api/src/modules/owner-applications/owner-applications.service.ts,
apps/api/src/modules/users/{users.service,users.controller}.ts,
apps/web/src/app/(tenant)/profile/page.tsx
คำขอ: เดิม "อุปกรณ์ที่เข้าสู่ระบบ" เป็นแค่ badge "เร็วๆ นี้" ไม่มีจริง user ถามว่า
ยากมั้ยถ้าจะทำจริง — ตอบไปว่าดูประวัติอย่างเดียวง่าย แต่ถ้าต้องเตะออกจาก
อุปกรณ์อื่นได้จริงต้องเพิ่ม session tracking เพราะระบบเดิมเป็น JWT stateless
ล้วน (ไม่เช็คอะไรกับ DB เลย token อายุ 7 วันตายตัว) user ให้ทำเวอร์ชันเต็ม
แก้ไข: เพิ่ม Session model (jti ผูกกับ token ที่ออกไปแต่ละครั้ง, userAgent, ip,
lastSeenAt, revokedAt) — ทุกจุดที่ sign JWT ให้ user (auth.service login/
register/adminLogin/loginWithGoogle, owner-applications finish) สร้าง
Session ใหม่พร้อม jti แล้วฝัง jti ลง JWT payload ด้วย จุดสำคัญ: JwtStrategy.
validate() (รันทุก request ที่ผ่าน JwtAuthGuard อยู่แล้วเป็นปกติ) เพิ่มเช็ค 1
จุดเดียว — ถ้า payload มี jti ให้ query Session ว่า revokedAt เป็น null ไหม
ถ้าไม่เจอหรือถูก revoke แล้วโยน 401 ทันที (ไม่ต้องรอ token หมดอายุเอง) — เป็น
การเพิ่มแบบ additive ไม่ต้องรื้อ auth guard เดิมทั้งระบบ, token เก่าที่ไม่มี jti
(ออกก่อนอัปเดตนี้) ยังใช้งานได้ปกติไม่บังคับ logout ทุกคนทันที
เพิ่ม GET /users/me/sessions (list เฉพาะที่ยังไม่ revoke), DELETE
/users/me/sessions/:id (เช็ค ownership ก่อนเสมอ) — หน้าเว็บแทนที่ badge
"เร็วๆ นี้" ด้วยรายการอุปกรณ์จริง (parse browser/OS หยาบๆ จาก User-Agent,
โชว์เวลาใช้งานล่าสุด) พร้อมปุ่ม "ออกจากระบบอุปกรณ์นี้" ต่อรายการ
สถานะ: แก้แล้ว + ทดสอบผ่าน curl+browser จริงครบ (login→jti ฝังใน token→list
session เห็นจริง→revoke→ใช้ token เดิมซ้ำโดน 401 ทันที→token เก่าไม่มี jti ยัง
ผ่านปกติ→ทดสอบกดปุ่มจริงในเบราว์เซอร์ revoke เซสชันตัวเอง redirect ไป
/login?error=session_expired ทันทีถูกต้อง) tsc --noEmit ผ่าน 0 error ทั้ง 2
apps (27/07/2026)

[50] หน้าส่งคำขอจอง /bookings/new ทำใหม่ตาม design + เพิ่ม GET /rooms/:id ที่ยังไม่มี
--------------------------------------------------------
ไฟล์: apps/api/src/modules/rooms/{rooms.service,rooms.controller,rooms.module}.ts,
apps/web/src/app/(tenant)/bookings/new/page.tsx
คำขอ: ทำหน้า /bookings/new ใหม่ตามแบบใน Claude Design (Hopak User Booking.dc.html)
ปัญหาที่เจอระหว่างทาง: หน้าเดิมเป็นฟอร์มเปล่าๆ ไม่โชว์ข้อมูลห้อง/หอ/ราคาเลย
ผู้เช่ากดจองโดยไม่เห็นว่ากำลังจองห้องไหน ราคาเท่าไหร่ ต้องจ่ายอะไรบ้าง — สาเหตุ
คือ **ไม่มี endpoint ดึงห้องเดี่ยวเลยทั้งระบบ** (มีแค่ GET /dorms/:dormId/rooms
ที่คืนทั้งหอ) หน้านี้รับมาแค่ ?roomId= จึงไม่มีทางรู้อะไรเพิ่ม
แก้ไข:
- เพิ่ม GET /rooms/:id (สาธารณะ) คืนห้อง + ข้อมูลหอ + คะแนนรีวิว — กรองทั้ง
  room.approved และ dorm.status='APPROVED' เหมือน listByDorm ไม่งั้นจะรั่วห้อง/หอ
  ที่ยังไม่ผ่านตรวจออกทาง endpoint ใหม่นี้แทน (ช่องโหว่เดียวกับที่เคยแก้ใน [37])
- RoomsModule import ReviewsModule เพื่อใช้ summaryForDorms คำนวณดาว/จำนวนรีวิว
  แบบเดียวกับที่ DormsService.findOne ใช้อยู่ (ไม่เขียนสูตรคำนวณซ้ำ)
- หน้าเว็บทำใหม่ตาม design: ปุ่มย้อนกลับ + หัวข้อ, stepper 5 ขั้น (ส่งข้อมูล →
  รอหอพักยืนยัน → หอพักยืนยัน → โอนเงิน+สลิป → รับใบเสร็จ) ไฮไลต์ขั้นที่ 1,
  layout 2 คอลัมน์ (ฟอร์มซ้าย / สรุปค่าใช้จ่ายแบบ sticky ขวา), ปุ่มวันที่แนะนำ 3 ตัว
  (วันที่ 1 และ 15 ของเดือนถัดไป, วันที่ 1 ของเดือนถัดไปอีกเดือน) + ช่องเลือกวันอื่นเอง,
  การ์ดสรุป 3 ข้อว่าหลังกดส่งแล้วจะเกิดอะไรต่อ
จุดที่จงใจไม่ทำตาม design (เพราะ design ไม่ตรงกับระบบจริง):
- design มีช่อง "ระยะเวลาเช่า 3/6/12 เดือน" — Booking ในระบบจริงไม่มีฟิลด์นี้เลย
  ถ้าใส่ไปจะเป็นช่องหลอกที่กรอกแล้วข้อมูลหายทันที จึงตัดออก
- design โชว์ "ยอดชำระวันเข้าอยู่ = ค่าเช่าเดือนแรก + ค่ามัดจำ" แต่ของจริง
  BookingsService.create ตั้ง amount = room.pricePerMonth เฉยๆ ไม่รวมมัดจำ
  (มัดจำไม่เคยถูกเก็บผ่าน Hopak เลย) — ถ้าโชว์ยอดรวมตาม design ผู้เช่าจะเข้าใจผิด
  ว่าต้องโอนเท่านั้นแล้วเจอเรียกเก็บเพิ่มทีหลัง จึงแยกบรรทัดมัดจำออกมาพร้อม
  กำกับว่า "ชำระกับหอโดยตรง" และยอดรวมใช้ชื่อ "ยอดชำระผ่าน Hopak" ให้ตรงของจริง
สถานะ: แก้แล้ว + ทดสอบผ่าน curl จริง (GET /rooms/:id คืนห้อง+หอ+รีวิวถูกต้อง) +
กดจริงในเบราว์เซอร์ครบ (โหลดข้อมูลห้องจริงขึ้นครบ ราคา ฿1,800 มัดจำ ฿3,500 รูปห้อง
จริง → กดปุ่มวันที่แนะนำ ค่าเข้า input จริง 2026-08-15 → กดส่งคำขอ สร้าง booking
สำเร็จ redirect ไป /bookings/{id} ถูกต้อง) ลบ booking ทดสอบแล้ว
tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (27/07/2026)

[51] ทั้งระบบไม่รองรับจอเล็ก — เมนูข้างกินจอมือถือจนใช้งานไม่ได้ + ตาราง/ฟอร์มล้น
--------------------------------------------------------
ไฟล์: apps/web/src/app/(admin)/admin/layout.tsx, (partner)/partner/layout.tsx,
components/dashboard/FilterTabs.tsx, ตาราง 14 จุดใน 11 หน้า, modal 4 หน้า,
(admin)/admin/{users,bookings,finance,website,dashboard}/page.tsx,
(partner)/partner/dashboard/page.tsx, (tenant)/{profile,+8 หน้า}/page.tsx
คำขอ: ให้ทุกหน้ารองรับทุกขนาดจอ (มือถือ / แท็บเล็ต / โน้ตบุ๊ก / คอม)
วิธีตรวจ: เขียนสคริปต์ audit ยิงทุกหน้า (33 หน้า) x 4 ความกว้าง (390/820/1024/
1440) ผ่าน CDP วัดจริง 3 อย่าง — (ก) document scrollWidth เกิน viewport ไหม
(ข) มี element ไหนที่เนื้อหากว้างเกินกล่องแล้วโดน overflow:hidden ตัดทิ้ง
(ค) พื้นที่เนื้อหาหลัก (<main>) แคบผิดปกติเทียบกับความกว้างจอไหม
**บทเรียนสำคัญ**: probe รอบแรกวัดแค่ scrollWidth ระดับ document แล้วได้ผลว่า
"ไม่พบปัญหาเลย" ซึ่งผิด — เพราะ layout ของ admin/partner ใช้ h-screen
overflow-hidden เนื้อหาที่ล้นจึงถูก "ตัดทิ้ง" ไม่ใช่ "ดันจอให้เลื่อนได้"
document เลยไม่เคยกว้างเกิน viewport ต้องเพิ่มการวัดข้อ (ข)+(ค) ถึงจะจับได้จริง
บั๊กที่เจอและแก้:
- [ร้ายแรง] admin + partner layout: sidebar เป็น `w-64 shrink-0` ตายตัวเสมอ
  บนจอ 390px กิน 256px เหลือพื้นที่เนื้อหาแค่ ~134px ใช้งานไม่ได้เลยทั้ง 2 ระบบ
  แก้เป็นลิ้นชัก (fixed + translate-x) เปิดด้วยปุ่มแฮมเบอร์เกอร์บนจอเล็ก
  พร้อมฉากหลังทึบกดปิดได้ และ `lg:static lg:translate-x-0` ให้จอใหญ่เหมือนเดิม
  เป๊ะ + ปิดลิ้นชักอัตโนมัติทุกครั้งที่เปลี่ยนหน้า (ไม่งั้นเมนูค้างทับหน้าใหม่)
- ตาราง 14 จุด: อยู่ใน overflow-x-auto อยู่แล้วแต่ตัว <table> เป็น w-full เฉยๆ
  บนมือถือจึงถูกบีบจนคอลัมน์ทับกันแทนที่จะเลื่อนดูได้ เพิ่ม min-w-[720px]
- modal 16 จุด: ไม่จำกัดความสูง บนจอเตี้ย/แนวนอนเนื้อหาล้นออกนอกจอ กดปุ่มยืนยัน
  ไม่ได้เลย เพิ่ม max-h-[85vh] overflow-y-auto + ลด padding ฉากหลังบนมือถือ
- admin/users: แถบ filter + ช่องค้นหา + ปุ่มเพิ่มผู้ใช้ อยู่ใน justify-between
  แถวเดียว บนมือถือ filter ถูกบีบเป็นคอลัมน์ผอมและปุ่มล้นออกนอกจอ ให้ซ้อนลงมา
  ก่อนแล้วค่อยเรียงแถวที่ lg (แก้แบบเดียวกันที่ admin/bookings, admin/finance)
- FilterTabs (ใช้ร่วมหลายหน้า): เพิ่ม whitespace-nowrap กันป้ายไทยแตกกลางคำ
- admin/partner dashboard: กราฟแบ่ง 2 คอลัมน์ที่ lg (1024) แต่ sidebar กิน 256px
  เหลือ 697px กราฟล้นออกนอกการ์ด เลื่อนไปแบ่งที่ xl แทน
- admin/website: ช่องเลือกไฟล์ + ปุ่มอัปโหลดเรียงแนวนอนล้นจอมือถือ ให้ซ้อนลงมา
- tenant profile: การ์ด "เปิดหอพักกับ Hopak" และ "ยืนยันตัวตนด้วย OTP" บีบข้อความ
  เป็นคอลัมน์ผอมข้างปุ่ม ให้ซ้อนลงมาบนมือถือ
- ระยะขอบหน้า tenant 9 หน้า: p-6/px-6 คงที่ กินพื้นที่จอ 390 มากไป ลดเหลือ 16px
  แล้วขยายที่ sm ขึ้นไป
ที่ตรวจแล้วไม่ใช่บั๊ก (ไม่แก้): วงกลม/เงา decorative ที่ตั้งใจให้ถูก overflow-hidden
ตัดขอบ (หน้าแรก/login/register/search/profile), canvas ของ Google Maps ที่วาด
กว้างกว่ากรอบตามปกติ, <text> ใน SVG กราฟที่เกิน 4px, และหน้าที่จงใจจำกัดความกว้าง
อ่านง่าย (bookings/[id], /pay, /notifications ใช้ max-w-sm/2xl/3xl จัดกึ่งกลาง)
สถานะ: แก้แล้ว + audit ซ้ำจนเหลือแต่รายการ benign ข้างต้น + เช็คหน้าจริงด้วย
ภาพหน้าจอที่ 390/820/1024 (ลิ้นชักเมนูเปิด-ปิดได้จริง, ตารางเลื่อนดูได้, การ์ด
เรียงลงมาถูกต้อง, กราฟไม่ล้น) tsc --noEmit ผ่าน 0 error (28/07/2026)

[52] [ร้ายแรง] GET /bookings/:id ไม่เช็คเจ้าของเลย — ใครล็อกอินก็อ่านการจองคนอื่นได้ทั้งหมด
--------------------------------------------------------
ไฟล์: apps/api/src/modules/bookings/{bookings.service,bookings.controller}.ts
อาการ: endpoint นี้เรียก findOne(id) ตรงๆ แล้วคืนทั้งแถวโดยไม่ตรวจอะไรเลย
ผู้ใช้คนไหนก็ได้ที่ล็อกอินอยู่ เดา/ไล่ booking id แล้วอ่านได้หมด — ชื่อผู้เช่า
เบอร์โทร ยอดเงิน หมายเหตุ ข้อมูลหอ ครบทุกฟิลด์
เจอตอน: กำลังจะเพิ่ม checkInToken (บั๊ก [53]) ลงในแถว Booking พอดี ถ้าไม่แก้ก่อน
โทเค็นยืนยันการเข้าพักจะรั่วออกไปทั้งระบบทันทีที่เพิ่มฟิลด์
แก้ไข: แยก findOneFor(id, user) สำหรับ endpoint สาธารณะออกจาก findOne() ที่
service อื่นเรียกใช้ภายใน — อนุญาตเฉพาะผู้เช่าเจ้าของการจอง, เจ้าของหอนั้น,
หรือแอดมิน นอกนั้น 403
สถานะ: แก้แล้ว + ทดสอบผ่าน curl (ผู้เช่าเจ้าของ 200 / เจ้าของหอนั้น 200 /
แอดมิน 200 / เจ้าของหออื่น 403 ถูกต้อง) (28/07/2026)

[53] ระบบยืนยันการเข้าพักด้วยโทเค็นจากใบเสร็จ (ปิดงานการจองเป็น COMPLETED)
--------------------------------------------------------
ไฟล์: apps/api/prisma/schema.prisma (migration add_checkin_token),
apps/api/src/modules/bookings/{bookings.service,bookings.controller}.ts,
apps/api/src/modules/admin/finance/finance.service.ts,
packages/shared/src/types/booking.ts,
apps/web/src/app/(tenant)/bookings/[id]/page.tsx,
apps/web/src/app/(partner)/partner/check-in/page.tsx (ใหม่),
apps/web/src/app/(partner)/partner/layout.tsx
คำขอ: ตาม design (Hopak User Booking.dc.html ที่อัปเดตใหม่ เพิ่มบล็อก "โทเค็น
ยืนยันการเข้าพัก" ในใบเสร็จขั้นที่ 5) — ผู้เช่าเอาโค้ดจากใบเสร็จไปยืนยันกับ
เจ้าของหออีกทีตอนเข้าพักจริง เพื่อปิดงาน
ปัญหาเดิม: BookingStatus มี COMPLETED อยู่แล้วในสคีมา แต่ **ไม่มีโค้ดที่ไหนเลย
ที่เซ็ตสถานะนี้** การจองจึงค้างที่ PAID ตลอดไป ไม่มีจุดจบของ flow
แก้ไข:
- เพิ่ม Booking.checkInToken (unique) / checkInTokenExpiresAt / checkedInAt
- ออกโค้ดตอน settlePayment() (จังหวะที่ใบเสร็จเกิดขึ้นพอดี) รูปแบบ HPK-XXXX-XXXX
  สุ่มด้วย crypto.randomInt จากชุดอักษรที่ตัด I O 0 1 ออก (คนต้องอ่านจากจอแล้ว
  พิมพ์/บอกปากเปล่ากันหน้าเคาน์เตอร์ ตัวที่สับสนกันได้ต้องไม่มี)
  เช็คโค้ดเดิมก่อนเสมอ กันกรณี settle ถูกเรียกซ้ำแล้วออกโค้ดใหม่ทับของที่ผู้เช่าถืออยู่
- อายุโค้ด = วันเข้าอยู่ + 7 วัน (design เขียนว่าหมดอายุวันเข้าอยู่พอดี แต่ถ้าทำตาม
  เป๊ะ คนมาสายแค่ข้ามเที่ยงคืนก็เข้าไม่ได้แล้ว จึงเผื่อไว้ 7 วัน)
- POST /bookings/check-in (เจ้าของหอเท่านั้น) รับแค่ "โค้ด" ไม่รับ bookingId —
  เพราะจุดประสงค์คือให้โค้ดเป็นตัวพิสูจน์ ไม่ใช่ให้เจ้าของหอเลือกเองว่าจะปิดอันไหน
  ตรวจ: โค้ดต้องอยู่ในหอของตัวเอง / ยังไม่ถูกใช้ / สถานะ PAID / ยังไม่หมดอายุ
  ผ่านแล้วเซ็ต COMPLETED + checkedInAt แล้ว emit realtime กลับหาผู้เช่า
  กรณีโค้ดไม่มีจริง กับ กรณีเป็นโค้ดของหออื่น ใช้ข้อความ error เดียวกัน —
  กันเจ้าของหอสุ่มโค้ดแล้วอ่านจากข้อความว่าโค้ดไหนมีอยู่จริงในระบบ
- **โทเค็นเห็นได้เฉพาะผู้เช่าเจ้าของการจองเท่านั้น** เจ้าของหอ/แอดมินเรียก API
  แล้วฟิลด์นี้จะถูกตัดออก ถ้าเจ้าของหออ่านโค้ดเองได้จากระบบ การ "ยืนยันตัวตน
  ตอนเข้าพัก" ก็ไม่เหลือความหมายเลย (ต่อยอดจาก [52] ที่แก้ ownership check)
- frontend: หน้า /bookings/[id] เดิมเป็นรายการข้อความเปล่าๆ ไม่มีใบเสร็จเลย
  ทำใบเสร็จตาม design (หัวไล่สี เลขที่ รายการ ยอดรวม แถบยืนยันโดยแอดมิน) +
  บล็อกโทเค็นเส้นประพร้อมปุ่มคัดลอกและวันหมดอายุ · ซ่อนบล็อกโทเค็นเมื่อเข้าพักแล้ว
  และเปลี่ยนเป็นข้อความยืนยันพร้อมเวลาที่เข้าพักแทน
- frontend: หน้าใหม่ /partner/check-in (เมนู "ยืนยันเข้าพัก" ในแถบข้าง) ช่องกรอก
  โค้ดตัวใหญ่ กรอกตัวเล็กก็ได้ระบบแปลงให้ + แสดงผลลัพธ์ครบ (ผู้เช่า เบอร์โทร หอ
  วันเข้าอยู่ ยอดที่ชำระ เวลายืนยัน) พร้อมปุ่มยืนยันรายการถัดไป
สถานะ: แก้แล้ว + ทดสอบ E2E จริงครบทั้ง flow ผ่าน curl (จอง→เจ้าของยืนยัน→ผู้เช่า
แนบสลิป→แอดมินเคลียร์บิล→โค้ดออกจริงในฐานข้อมูล) และเคสขอบครบ (ผู้เช่าเห็นโค้ด /
เจ้าของหอกับแอดมินไม่เห็น / คนนอก 403 / ผู้เช่ากดยืนยันเองไม่ได้ 403 / เจ้าของหอ
คนอื่นใช้โค้ดไม่ได้ / โค้ดมั่วไม่ได้ / โค้ดว่างไม่ได้ / พิมพ์ตัวเล็กมีช่องว่างยังผ่าน /
ใช้ซ้ำไม่ได้ / สถานะเปลี่ยนเป็น COMPLETED จริง) + กดจริงในเบราว์เซอร์ทั้ง 2 ฝั่ง
(ใบเสร็จโชว์โค้ด HPK-GFWH-LHHY หมดอายุ 22 ส.ค. 2569 ถูกต้อง, เจ้าของหอกรอกโค้ดผิด
ขึ้น error, กรอกถูกขึ้นผลลัพธ์ครบ) ลบข้อมูลทดสอบแล้ว tsc --noEmit ผ่าน 0 error
ทั้ง 2 apps (28/07/2026)

[54] ฝั่งเจ้าของหอไม่มีหน้าจัดการโปรไฟล์เลย + วงกลม avatar เป็นแค่ตัวอักษรย่อ กดไม่ได้
--------------------------------------------------------
ไฟล์: apps/web/src/app/(partner)/partner/profile/page.tsx (ใหม่),
apps/web/src/app/(partner)/partner/layout.tsx
อาการ/คำขอ: วงกลมโปรไฟล์มุมขวาบนของ dashboard เจ้าของหอเป็นแค่ตัวอักษรย่อ ("น้")
ไม่โชว์รูปที่อัปโหลดไว้ และกดไม่ได้ — เจ้าของหอไม่มีทางแก้ชื่อ/เบอร์/รูป/รหัสผ่าน
ของตัวเองได้เลยจากฝั่ง partner (หน้า /partner/settings มีแค่บัญชีรับเงิน กับ
ชื่อเจ้าของ/ชื่อหอแบบอ่านอย่างเดียว) ต้องไปใช้หน้า /profile ของฝั่งผู้เช่าแทน
ซึ่งอยู่คนละ layout กัน (มี Navbar ผู้เช่า ไม่มีเมนูเจ้าของหอ)
หมายเหตุ: endpoint ฝั่ง API มีครบอยู่แล้วทุกตัว (POST /users/me/avatar,
PATCH /users/me, PATCH /users/me/password) — ขาดแค่หน้าเว็บฝั่ง partner ที่เรียกใช้
แก้ไข:
- เพิ่มหน้า /partner/profile: การ์ดหัวมีรูปโปรไฟล์กดเปลี่ยนได้ (hover ขึ้น
  "เปลี่ยนรูป") + ป้ายสถานะยืนยันอีเมล + รายชื่อหอของตัวเอง, ฟอร์มแก้ชื่อ/เบอร์,
  ฟอร์มเปลี่ยนรหัสผ่าน, และลิงก์ไปหน้าตั้งค่าสำหรับบัญชีรับเงิน (ไม่ทำซ้ำ)
  ช่องอีเมลตั้งเป็นอ่านอย่างเดียว เพราะ UpdateProfileDto ไม่รับ email อยู่แล้ว
  ถ้าปล่อยให้พิมพ์ได้จะกลายเป็นช่องหลอกที่กรอกแล้วไม่มีอะไรเกิดขึ้น
- วงกลม avatar ทั้ง 2 จุด (มุมขวาบน + ท้ายแถบเมนูข้าง) โชว์ user.avatarUrl จริง
  (ไม่มีรูปค่อย fallback เป็นตัวย่อ) และเปลี่ยนเป็นลิงก์ไป /partner/profile
  ท้ายแถบเมนูเปลี่ยนข้อความรองจาก "เจ้าของหอ" เป็น "จัดการโปรไฟล์" ให้รู้ว่ากดได้
สถานะ: แก้แล้ว + ทดสอบจริง (กดวงกลม avatar แล้วไป /partner/profile จริง, แก้ชื่อ
กดบันทึกแล้วค่าเปลี่ยนในฐานข้อมูลจริง, อัปโหลดรูปใหม่ผ่าน endpoint จริงแล้ว
avatarUrl เปลี่ยนจริง) คืนค่าชื่อ/รูปเดิมและลบไฟล์รูปทดสอบทิ้งแล้ว
tsc --noEmit ผ่าน 0 error (28/07/2026)

========================================================
[ตรวจสอบความปลอดภัยทั้งระบบ 28/07/2026] ไล่ทุก controller + ยิงทดสอบจริงด้วย curl
========================================================
วิธีตรวจ: ไล่ทุก endpoint (13 โมดูล) ดู guard/role/ownership แล้วยิงทดสอบจริงด้วย
token ผู้ใช้ tenant ปกติ (จำลองผู้โจมตี) เฉพาะที่สงสัย — ไม่รายงานถ้ายังไม่ยืนยันว่าเจาะได้

ยืนยันแล้วว่าเจาะได้จริง (curl ผ่าน):

[55] [ร้ายแรง] POST /bookings/:bookingId/payment ไม่เช็คเจ้าของ + ไม่เช็คสถานะ booking
--------------------------------------------------------
ไฟล์: apps/api/src/modules/payments/payments.service.ts (pay)
ปัญหา 2 อย่างในจุดเดียว:
1) IDOR — pay() รับ bookingId แล้วสร้าง Payment เลย ไม่เช็คว่า booking นั้นเป็นของ
   ผู้ที่ล็อกอินอยู่หรือไม่ ผู้เช่าคนไหนก็จ่ายเงิน/แนบสลิปทับ booking ของคนอื่นได้
   (ทดสอบจริง: tenant A แนบสลิปลง booking ของ tenant B สำเร็จ 201)
2) ข้ามขั้นตอน — ไม่เช็คว่า booking.status เป็น CONFIRMED ก่อน จ่าย booking ที่ยัง
   PENDING (เจ้าของหอยังไม่ยืนยัน) ได้เลย (ทดสอบจริง: จอง→จ่ายทันที 201 โดยข้าม
   การยืนยันของเจ้าของหอ) พอแอดมินเคลียร์บิลทีหลังจะกลายเป็น PAID + ห้อง OCCUPIED +
   ออกโค้ดเข้าพัก ทั้งที่เจ้าของหอไม่เคยกดยืนยัน — ระบบ state machine ที่คุม 
   confirm/reject/cancel อยู่ ถูกข้ามหมดเพราะ payment ไม่เรียกใช้มัน
ผลกระทบ: ก่อกวน (ยัดสลิปมั่วลง booking คนอื่น), บิดเบือนสถานะการจอง, ข้ามการอนุมัติ
ของเจ้าของหอ
แนวทางแก้ (ยังไม่ทำ รอตัดสินใจ): payments.controller ส่ง @CurrentUser().id เข้ามา
แล้ว pay() เช็ค booking.tenantId===userId และ booking.status==='CONFIRMED' ก่อนสร้าง
Payment + กันจ่ายซ้ำถ้ามี payment อยู่แล้ว
สถานะ: ยืนยันช่องโหว่แล้ว ยังไม่แก้ (ลบ payment/booking ทดสอบทิ้งแล้ว)

[56] [กลาง] POST /uploads เปิดให้ผู้ใช้ล็อกอินอัปโหลดไฟล์ชนิดใดก็ได้ เสิร์ฟเป็น text/html (stored XSS)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/uploads/uploads.controller.ts
ปัญหา: endpoint นี้รับไฟล์ทุกชนิดไม่จำกัด mimetype/นามสกุล เก็บแบบ public แล้ว
เสิร์ฟผ่าน /uploads/ (useStaticAssets) โดย Content-Type ตามนามสกุลไฟล์ — อัปโหลด
.html ที่มี <script> แล้วเปิดลิงก์ สคริปต์รันจริง (ทดสอบจริง: เสิร์ฟกลับมาเป็น
text/html; charset=UTF-8) เช่นกันกับ .svg ที่ฝัง script
บรรเทาความรุนแรง: ไฟล์เสิร์ฟจาก origin ของ API (:4000) ซึ่งไม่มี cookie/session
(ระบบใช้ JWT ใน localStorage ของเว็บ :3001) XSS บน origin API จึงเข้าถึง token ไม่ได้ตรงๆ
จุดสำคัญ: **grep แล้วไม่มีหน้าเว็บไหนเรียก POST /uploads เลย เป็น dead endpoint**
แนวทางแก้ที่ปลอดภัยสุด: ลบ endpoint นี้ทิ้ง (ไม่มีใครใช้) — หรือถ้าจะเก็บไว้ ต้อง
whitelist เฉพาะรูปภาพ + บังคับ Content-Disposition: attachment
สถานะ: ยืนยันช่องโหว่แล้ว ยังไม่แก้ (ลบไฟล์ทดสอบทิ้งแล้ว)

[57] [กลาง] ไม่มี rate limiting ที่ endpoint ไหนเลย (login / OTP / forgot-password)
--------------------------------------------------------
ไฟล์: apps/api/src/main.ts (ไม่มี ThrottlerModule ทั้งโปรเจกต์)
ปัญหา: /auth/login เดารหัสผ่านได้ไม่จำกัดจำนวนครั้ง, /auth/forgot-password และ
/users/me/send-verification-otp ยิงสแปมอีเมลได้ (มี cooldown 60 วิ ต่อ "บัญชี" แต่
ยิงคนละอีเมลรัวๆ ได้) — OTP verify มี lock 5 ครั้งแล้ว (ดี) แต่ password login ไม่มี
แนวทางแก้ (ยังไม่ทำ): เพิ่ม @nestjs/throttler ครอบ endpoint กลุ่ม auth
สถานะ: ยืนยันแล้ว ยังไม่แก้

[58] [ต่ำ] CORS เปิดกว้างทุก origin (app.enableCors() ไม่มี argument)
--------------------------------------------------------
ไฟล์: apps/api/src/main.ts บรรทัด 9
ปัญหา: เว็บไซต์ไหนก็เรียก API ได้ — ความเสี่ยงจำกัดเพราะใช้ JWT bearer (ไม่ใช่
cookie) จึงไม่โดน CSRF ตรงๆ แต่ถ้า token หลุดไปเว็บอื่น เรียกใช้ได้ทันที
แนวทางแก้ (ยังไม่ทำ): จำกัด origin เป็น FRONTEND_URL ของ production
สถานะ: ยืนยันแล้ว ยังไม่แก้ (ความเสี่ยงต่ำ)

--- จุดที่ตรวจแล้วปลอดภัยดี (ไม่ใช่ช่องโหว่) ---
- GET /bookings/:id เช็ค ownership แล้ว (เพิ่งแก้ [52])
- โทเค็นเข้าพักเห็นเฉพาะผู้เช่าเจ้าของ (เพิ่งทำ [53])
- POST review เช็คว่าเคยพักจริง (status PAID/COMPLETED) ก่อนรีวิว — กันรีวิวปลอม
- notifications markRead / getAttachment เช็ค userId เจ้าของก่อนทุกครั้ง
- path traversal ที่ /uploads กันได้ด้วย timestamp prefix (../ ถูกทำให้เป็นชื่อไฟล์)
- OTP ทั้ง users และ owner-applications มี lock 5 ครั้ง + หมดอายุ 10 นาที
- /users/me ไม่หลุด password/otpCodeHash/resetTokenHash (omitPassword ตัดออกครบ)
- admin-login ตรวจ role !== ADMIN คืน 401 — tenant ยิงเข้าไม่ได้
- ไฟล์เอกสารส่วนตัว (บัตร/โฉนด) เสิร์ฟผ่าน token เซ็น HMAC หมดอายุ 15 นาที เท่านั้น
- .env / .env.production ไม่ถูก track ใน git (.gitignore ครอบไว้)
- ทุก endpoint admin/* มี JwtAuthGuard + RolesGuard('admin') ครบ
- owner-applications ไม่มี auth โดยตั้งใจ (id cuid สุ่มทำหน้าที่ bearer ก่อนมีบัญชี)
  — เป็น design ที่ยอมรับได้ เอกสารเข้าถึงผ่าน signed URL หมดอายุเหมือนกัน

[59] แอดมินแก้ข้อความการ์ดจุดขาย 3 ใบใต้ hero หน้าแรกได้เอง (เดิม hardcode ในโค้ด)
--------------------------------------------------------
ไฟล์: apps/api/prisma/schema.prisma (migration add_promo_cards),
apps/api/src/modules/settings/{settings.service,settings.controller}.ts,
apps/web/src/app/page.tsx, apps/web/src/app/(admin)/admin/website/page.tsx
คำขอ: การ์ด 3 ใบใต้แถบค้นหาหน้าแรก ("ยืนยันตัวตนแล้ว / สมาชิกใหม่ / โปร่งใส")
เดิมเป็นข้อความ hardcode ใน t.promos แก้ได้เฉพาะโปรแกรมเมอร์ — เจ้าของระบบขอให้
แอดมินแก้เองได้จากหน้า /admin/website
แก้ไข:
- schema: เพิ่ม SiteSettings.promoCards (Json array) เก็บ [{tagTh,titleTh,subTh,
  tagEn,titleEn,subEn}] — เก็บทั้งไทยและอังกฤษในใบเดียว
- getHero() คืน promoCards เพิ่ม, POST /admin/settings/promos (admin เท่านั้น)
  setPromoCards() cap ที่ 3 ใบ + trim ทุก field + ถ้าส่ง [] = กลับไปใช้ default
- หน้าแรก: merge ต่อช่อง — คงไว้ 3 ใบเสมอ ช่องไหนแอดมินตั้ง title ไว้ (ตามภาษา
  ที่ดู) ใช้ของแอดมิน ที่เหลือใช้ข้อความ default ของช่องนั้น
- หน้า admin/website: เพิ่มการ์ดฟอร์ม 3 ใบ แต่ละใบมี tag/title/sub ทั้งไทยและ
  อังกฤษ + ปุ่มบันทึก + ปุ่มล้างทั้งหมด(กลับ default)
จุดที่เกือบพลาด (แก้แล้ว): เวอร์ชันแรก filter การ์ดว่างทิ้งก่อนส่ง ทำให้ index ยุบ —
ถ้าแอดมินแก้แค่ใบ 1 หน้าแรกจะเหลือการ์ดใบเดียว (อีก 2 หาย) แก้เป็นส่งทั้ง 3 ช่อง
เสมอถ้ามีอย่างน้อย 1 ใบกรอก แล้วหน้าแรก merge ต่อ index กับ default
สถานะ: แก้แล้ว + ทดสอบผ่าน curl (roundtrip, non-admin โดน 403, ส่งเกิน 3 ตัดเหลือ 3,
[] reset สำเร็จ) + E2E เบราว์เซอร์จริง (กรอกแค่ใบ 1 ในแอดมิน บันทึก → หน้าแรกโชว์
ครบ 3 ใบ ใบ1=ของแอดมิน "ลดค่ามัดจำ 50%" ใบ2,3=default ถูกต้อง) reset ข้อมูล
ทดสอบกลับว่างแล้ว tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (28/07/2026)

[60] หน้าแจ้งเตือน /notifications ทำใหม่ตาม design + กระดิ่งทุกที่ (เฉพาะผู้เช่าที่ล็อกอิน)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/notifications/{notifications.service,controller}.ts,
apps/api/src/modules/bookings/{bookings.service,bookings.module}.ts,
apps/api/src/modules/admin/finance/finance.service.ts,
apps/web/src/app/(tenant)/notifications/page.tsx,
apps/web/src/components/Navbar.tsx, apps/web/src/app/page.tsx
คำขอ: ทำหน้าแจ้งเตือนตาม design (Hopak User Notifications.dc.html) + กระดิ่ง
แจ้งเตือนทุกที่ โชว์เฉพาะตอนล็อกอินและเป็น user (ผู้เช่า) เท่านั้น
ปัญหาเดิมที่เจอ: ระบบสร้าง notification จริงแค่ 2 ชนิด — payout (ให้เจ้าของหอ) กับ
warning (ให้ผู้ใช้) ผู้เช่าแทบไม่มีอะไรเข้าหน้าแจ้งเตือนเลย เหตุการณ์การจอง
(เจ้าของยืนยัน/ปฏิเสธ, แอดมินเคลียร์บิล) ส่งแค่ socket 'booking:updated' ชั่วคราว
ไม่เคยบันทึกเป็น notification ถาวร
แก้ไข:
- hook สร้าง notification จริงให้ผู้เช่าตอนเหตุการณ์เกิด (ของจริง ไม่ใช่ข้อมูลปลอม):
  confirm booking (type 'booking'), reject booking (type 'booking'),
  settlePayment ของแอดมิน (type 'payment' — ใบเสร็จ+โทเค็นพร้อม)
- เพิ่ม markAllRead() + PATCH /notifications/read-all (วางก่อน :id/read กัน
  'read-all' ถูกจับเป็น id)
- design มีหมวด chat/promo แต่ระบบจริงไม่มีฟีเจอร์แชท/โปรโมชั่น จึงไม่ทำ (ไม่สร้าง
  ข้อมูลปลอม) — map ชนิดจริงเข้า tab: booking, payment, ระบบ(warning+payout)
- หน้าเว็บทำใหม่ตาม design: header + ตัวนับยังไม่อ่าน + ปุ่มอ่านทั้งหมด, filter
  tabs พร้อมตัวนับต่อหมวด, จัดกลุ่มตามเวลา (วันนี้/เมื่อวาน/ก่อนหน้า จาก createdAt
  จริง), จุดน้ำเงินรายการยังไม่อ่าน, ไอคอนแยกสีตามชนิด, empty state, เวลาแบบ
  relative (x นาที/ชม.ที่แล้ว)
- กระดิ่ง: เพิ่มใน Navbar.tsx (ใช้ทุกหน้า tenant) และ navbar หน้าแรก (page.tsx มี
  navbar แยกของตัวเอง) โชว์เฉพาะ user.role==='tenant' ที่ล็อกอินอยู่ + badge นับ
  ยังไม่อ่าน (99+ เมื่อเกิน) เพิ่มทันทีเมื่อมี notification:new เรียลไทม์
- ตัวนับ badge sync: รีเฟรชตอนเปลี่ยนหน้า + custom event 'hopak:notif-read'
  (dispatch ตอนกดอ่าน/อ่านทั้งหมด) กันตัวเลขค้างขณะอยู่หน้าเดียวกัน
สถานะ: แก้แล้ว + ทดสอบผ่าน curl (roundtrip, markAll → DB unread=0) + E2E เบราว์เซอร์
จริงครบ (หน้าโชว์ 4 รายการจัดกลุ่ม 3 ช่วงเวลา badge 3, filter การชำระเงินแยกถูก,
กดอ่านทั้งหมด header เป็น 0 จุดหาย ปุ่มซ่อน, owner ที่หน้าแรกไม่มีกระดิ่งถูกต้อง,
มือถือเรียงสวย) ลบข้อมูลทดสอบแล้ว tsc --noEmit ผ่าน 0 error ทั้ง 2 apps
ระหว่างทางแก้: .next cache พัง (Cannot find module _document.js) จากการ restart
dev หลายรอบ — ล้าง .next แล้วรันใหม่หาย (ไม่ใช่บั๊กในโค้ด)

[61] อัปเดตหน้ารายละเอียดหอ /dorms/:id ตาม design ใหม่ (การ์ดค่าใช้จ่าย + carousel + modal ห้อง)
--------------------------------------------------------
ไฟล์: apps/web/src/app/(tenant)/dorms/[id]/page.tsx, apps/web/src/lib/amenities.ts (ใหม่)
คำขอ: อัปเดตหน้ารายละเอียดหอตาม design (Hopak User Dorm Detail.dc.html)
ของใหม่ที่เพิ่มจากหน้าเดิม:
- การ์ด "ค่าใช้จ่าย" 3 ช่อง (ค่าไฟ/ค่าน้ำ/ค่ามัดจำ) กลับมาโชว์ในการ์ดหัว — แต่ใช้
  เรตระดับห้องจริง (cheapestRoom) ก่อน ตกไป dorm-level เฉพาะเมื่อห้องไม่ได้ตั้ง
  และโชว์เฉพาะเมื่อมีค่าจริง (hasCosts) — กันเคสหอที่ dorm-level เป็น 0 หมด
  (เคยเป็นเหตุให้ต้องเอาการ์ดนี้ออกใน [แก้ครั้งก่อน] เพราะโชว์ 0/0/0)
- carousel รูปห้องต่อกลุ่ม (แอร์/พัดลม) มีลูกศรซ้าย-ขวา + จุดบอกตำแหน่ง + เลข x/N
  รวมรูปห้องจริงทุกห้องในกลุ่ม (เดิมเป็น auto-rotate เฉยๆ ไม่มีปุ่ม)
- แถวห้องจัดเป็น "แบบห้อง" (รวมห้องชื่อ+ราคาเดียวกัน นับจำนวนว่าง) มี thumbnail +
  ป้าย "ว่าง N ห้อง" + "ดูรายละเอียดห้อง →" กดเปิด modal
- **modal รายละเอียดห้อง (ใหม่)**: gallery รูปห้องจริง + thumbnails เลือกได้,
  spec 4 ช่อง, สิ่งอำนวยความสะดวกในห้อง (ป้ายไทย/อังกฤษ + ไอคอน), ราคา + ปุ่มจอง
จุดที่จงใจไม่ตาม design (เพราะไม่มีข้อมูลจริง — ไม่ปั้นข้อมูลปลอม):
- design มี spec "ขนาดห้อง (ตร.ม.)" และ "ตำแหน่งชั้น" — ระบบไม่เก็บ 2 อย่างนี้เลย
  จึงแทนด้วย spec ที่มีจริง: ห้องว่าง / ค่ามัดจำ / ค่าไฟ / ค่าน้ำ
- design มีปุ่มแชทหาเจ้าของหอ — ระบบไม่มีฟีเจอร์แชท จึงไม่ทำ (คงการ์ดเจ้าของหอ
  แบบแสดงชื่ออย่างเดียวเหมือนเดิม)
- ป้าย amenity: ดึง AMENITY_ICON/LABEL ออกมาเป็น lib/amenities.ts ใช้ร่วมกับหน้า
  เพิ่มห้อง (partner/rooms/new) — โค้ด ac/wifi/bath/... แปลงเป็นป้ายไทย/อังกฤษ
สถานะ: แก้แล้ว + ทดสอบเบราว์เซอร์จริงครบ (หน้าเต็มโชว์ค่าใช้จ่ายจริง ฿7/฿20/฿3,000,
carousel เลื่อนได้ 1/3, กดแถวห้องเปิด modal โชว์ 3 รูปจริง+specs จริง+amenities
ไทย+ปุ่มจอง, มือถือเรียงลงมาครบ) tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (28/07/2026)

[62] เจ้าของหอจองห้องของตัวเองไม่ได้ (backend guard + ซ่อนปุ่มจองในหน้าเว็บ)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/bookings/bookings.service.ts,
apps/web/src/app/(tenant)/dorms/[id]/page.tsx
อาการ: RolesGuard('tenant') กัน role owner ยิง POST /bookings อยู่แล้ว (403) แต่
หน้ารายละเอียดหอยังโชว์ปุ่ม "จองเลย" ให้เจ้าของเห็น กดแล้วเจอ error ดิบ — UX แย่
แก้ไข:
- backend: create() เพิ่มเช็ค room.dorm.ownerId===tenantId แล้ว throw 400 เป็นด่าน
  สำรอง (กันเคสขอบ เช่น บัญชีเปลี่ยน role ภายหลัง หรือถูกเรียกจากที่อื่น)
- frontend: เมื่อ isOwnerHere (ผู้ล็อกอินเป็นเจ้าของหอนี้) ซ่อนปุ่ม "จองเลย" ใน
  sidebar แทนด้วยการ์ด "นี่คือหอพักของคุณ · คุณจองห้องของตัวเองไม่ได้", และ
  ซ่อนปุ่ม "จองห้องนี้" ใน modal แทนด้วยข้อความเดียวกัน (isOwnerHere มีอยู่แล้ว
  จากฟีเจอร์ตอบรีวิว ใช้ซ้ำ)
สถานะ: แก้แล้ว + ทดสอบจริง (เจ้าของหอ POST /bookings ห้องตัวเองโดน 403, หน้าเว็บ
เจ้าของเห็น "นี่คือหอพักของคุณ" ไม่มีปุ่มจองทั้ง sidebar และ modal) tsc ผ่าน 0
error ทั้ง 2 apps (28/07/2026)

========================================================
[แก้ช่องโหว่ก่อน deploy 28/07/2026] แก้ [55]-[58] จากผลตรวจ security audit
========================================================

[55] แก้แล้ว: payment IDOR + ข้ามสถานะ
- payments.controller ส่ง @CurrentUser().id เข้า pay()
- pay() เช็ค 3 อย่างก่อนสร้าง Payment: (1) booking.tenantId===userId (ไม่ใช่ของ
  คุณ→403) (2) status==='CONFIRMED' (ยังไม่ยืนยัน→400) (3) ไม่มี payment เดิม (จ่าย
  ซ้ำ→400)
- ทดสอบ curl ครบ: จ่ายตอน PENDING บล็อก / คนอื่นจ่ายทับ 403 / เจ้าของจ่ายหลัง
  CONFIRMED ผ่าน / จ่ายซ้ำ 400

[56] แก้แล้ว: ลบ dead endpoint POST /uploads (stored XSS)
- ลบ uploads.controller.ts + เอาออกจาก uploads.module (เหลือ FilesController เสิร์ฟ
  ไฟล์ private ผ่าน signed URL) — grep ยืนยันไม่มี web/mobile เรียกเลย
- ทดสอบ: POST /uploads = 404 การอัปโหลดจริงไปผ่าน endpoint เฉพาะทางที่ whitelist
  ชนิดไฟล์อยู่แล้ว (avatar/รูปห้อง/เอกสาร)

[57] แก้แล้ว: เพิ่ม rate limit (zero-dep, in-memory fixed-window)
- common/guards/rate-limit.guard.ts + decorators/rate-limit.decorator.ts (@RateLimit)
- ไม่เพิ่ม npm dependency (ไม่ใช้ @nestjs/throttler) — เก็บใน Map คีย์ IP+route
  เหมาะกับ prod instance เดียว (สเกลหลาย instance ค่อยเปลี่ยนเป็น Redis)
- ใส่: login/register/admin-login/reset-password 10 ครั้ง/นาที, forgot-password +
  send-verification-otp 5 ครั้ง/นาที (ต่อ IP)
- ทดสอบ: forgot-password ครั้งที่ 6 โดน 429, login เกิน 10 โดน 429

[58] แก้แล้ว: จำกัด CORS + trust proxy
- main.ts: enableCors origin = FRONTEND_URL (prod) / localhost regex (dev)
- app.set('trust proxy', 1) — อยู่หลัง nginx ให้ req.ip เป็น IP จริง (จำเป็นต่อ rate
  limiter + เก็บ ip ใน Session)
- ทดสอบ: Origin evil.com ไม่ได้ ACAO header, localhost ได้

สถานะรวม: [55]-[58] แก้ครบ + ทดสอบ curl ผ่านทุกเคส tsc --noEmit ผ่าน 0 error
เหลือใน PRE-DEPLOY-CHECKLIST: D1 migrate deploy, D2 แก้ role คนเก่า, D3 volume
private (แก้ใน compose แล้ว รอ redeploy)

[63] [ความปลอดภัย] JWT token หลุดใน URL/nginx log/Referer ตอน Google login callback
--------------------------------------------------------
ไฟล์: apps/api/src/modules/auth/auth.controller.ts,
apps/web/src/app/(auth)/auth/google/callback/page.tsx
เจอตอน: ทดสอบเจาะ + ดู nginx access log จริง เห็นบรรทัด:
  GET /_next/.../callback/page-*.js ... Referer: .../auth/google/callback?token=eyJ...
อาการ: หลัง login ด้วย Google API redirect ไป
  FRONTEND_URL/auth/google/callback?token=<JWT> (ใส่ token ใน query string)
ทำให้ token 7 วันหลุดเข้า:
  - nginx access log (token อยู่ใน request line ทุกครั้ง)
  - Referer header → รั่วไปทุก resource (.js/.css/รูป) ที่โหลดบนหน้านั้น
  - browser history, proxy/CDN log
ผลกระทบ: ใครเข้าถึง log หรือดัก Referer ได้ = ขโมย token → เข้าบัญชีได้เลย
แก้ไข:
- API: เปลี่ยน redirect จาก query (?token=) เป็น URL fragment (#token=)
  fragment เบราว์เซอร์ไม่ส่งไป server จึงไม่เข้า access log และไม่รั่วผ่าน Referer
- web callback page: อ่าน token จาก window.location.hash แทน useSearchParams
  แล้ว window.history.replaceState ล้าง fragment ทิ้งจาก URL ทันทีหลังเก็บ token
  (กันค้างใน history/แชร์ลิงก์ต่อ)
สถานะ: แก้แล้ว + ยืนยันโค้ด redirect ใช้ # tsc ผ่าน 0 error ทั้ง 2 apps (28/07/2026)
หมายเหตุ: เป็น finding จากการทำ pentest จริง (blue team ดู nginx log เจอ) —
ตอกย้ำว่าควรมี security logging ในแอปเองด้วย (ยังเป็นช่องว่างที่ค้างอยู่)

[64] [ความปลอดภัย] Session ค้างไม่หมดอายุเอง + login ซ้อนหลายเครื่องพร้อมกันได้ (1 บัญชีหลาย session)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/auth/auth.service.ts (sign),
apps/api/src/modules/auth/strategies/jwt.strategy.ts,
apps/web/src/lib/auth.ts, apps/web/src/app/(auth)/login/page.tsx
อาการ: (1) session ที่ login แล้วอยู่ได้ยาวจนกว่า JWT หมดอายุ 7 วัน (หรือโดน
revoke เอง) — ไม่มี idle timeout เลย ถ้าเครื่องหลุดมือ/token ถูกขโมยไป ใช้ต่อได้
นานมาก (2) sign() สร้าง Session ใหม่ทุก login โดยไม่ล้างตัวเก่า → 1 บัญชีมี
session active หลายตัวพร้อมกัน คนที่รู้รหัสเดียวกัน login ค้างหลายเครื่องได้ ไม่มี
การเตะออก
แก้ไข (คำสั่งเจ้าของระบบ: ตัวใหม่เตะตัวเก่า + idle 30 นาที):
- sign(): ก่อนออก token ใหม่ ทำ $transaction revoke ทุก session ที่ยัง active
  ของ user คนนั้น (updateMany revokedAt=now) แล้วค่อย create แถวใหม่ →
  1 บัญชี = 1 session อุปกรณ์เก่าโดน 401 request ถัดไปทันที (newest-wins)
  ครอบทุกทางเข้า: login, adminLogin, register, loginWithGoogle (ทุกตัวเรียก sign)
- jwt.strategy validate(): เพิ่มเช็ค idle — ถ้า now - session.lastSeenAt เกิน
  30 นาที → set revokedAt แล้ว throw 401 "เซสชันหมดอายุจากการไม่ใช้งาน"
  (lastSeenAt อัปเดตทุก request อยู่แล้ว) = auto-logout เมื่อไม่ใช้งาน
- ฝั่ง client ไม่ต้องแก้เพิ่ม: api-client.ts จับ 401+token อยู่แล้ว → clearToken
  + เด้งไป /login?error=session_expired อัตโนมัติ (session หมดอายุ/โดนเตะ เด้งออกเอง)
- remember email (จำอีเมล ไม่จำรหัส): auth.ts เพิ่ม rememberLogin/getRememberedLogin/
  forgetLogin (เก็บ localStorage 'hopak_last_login' เฉพาะ identifier ไม่เคยเก็บรหัส)
  หน้า login: login สำเร็จ → rememberLogin(email); เปิดหน้า → prefill ช่องอีเมลให้
  อัตโนมัติ + chip "ใช้ล่าสุด: xxx" กดกรอกด่วน + ปุ่ม "ไม่ใช่คุณ?" ล้างทิ้ง
สถานะ: แก้แล้ว tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (29/07/2026)
หมายเหตุ: ไม่ต้อง migrate — Session มี lastSeenAt/revokedAt ครบอยู่แล้ว
ข้อควรรู้: newest-wins แปลว่าถ้า user เปิดพร้อมกัน 2 แท็บ/2 เครื่องแล้ว login ใหม่
เครื่องเดิมจะโดนเตะออก (ตามที่เลือกไว้) — เป็นพฤติกรรมตั้งใจ ไม่ใช่บั๊ก

[64 แก้ไขเพิ่ม 29/07/2026] เจ้าของระบบรายงาน: เปิดหลายบราวเซอร์แล้วยังล็อกอินอยู่ทุกอัน
(newest-wins เตะจริงฝั่ง backend — ยืนยันด้วย test: token เก่า→401) แต่บราวเซอร์เก่า
"ยังโชว์ล็อกอินอยู่" เพราะไม่ได้ยิง request ใหม่ เลยไม่รู้ว่าโดนเตะ (client เด้งออก
เฉพาะตอนเจอ 401 จาก api-client) — เป็นปัญหา visibility ฝั่ง client ไม่ใช่ backend
แก้: เพิ่ม heartbeat เฝ้า session
- backend: GET /auth/session (ไม่มี guard, รับ token ผ่าน header เอง) + checkSession()
  ใน auth.service — verify jwt + เช็ค revoked/idle คืน {valid} สำคัญ: ไม่ bump lastSeenAt
  (ต่างจาก request ผ่าน JwtStrategy) เพื่อให้ heartbeat เรียกถี่ได้โดยไม่กัน idle หมดอายุ
- frontend: components/SessionWatcher.tsx (mount ใน root layout) poll ทุก 15 วิ +
  ทุกครั้งที่สลับกลับมาที่แท็บ (visibilitychange/focus) ถ้า valid:false → clearToken +
  เด้ง /login?error=session_expired ทันที
ผล: บราวเซอร์เก่าโดนเตะออกจริงภายใน 15 วิ หรือทันทีตอนสลับแท็บ
ทดสอบ: /auth/session token เก่า valid:false / ใหม่ valid:true / ไม่มี token false
tsc ผ่าน 0 error ทั้ง 2 apps

[64 แก้ไขเพิ่ม 29/07/2026] เจ้าของระบบรายงาน: กดรีเฟรชแล้วหลุด (ไม่ควรหลุด — รีเฟรช=กำลังใช้งาน)
ต้นเหตุ: SessionWatcher เดิมเช็คด้วย /auth/session (ไม่ bump lastSeenAt) แม้ตอนโหลดหน้า/
รีเฟรช เลยไม่นับรีเฟรชเป็นกิจกรรม (idle เดินต่อ) + บางครั้งหลุดเพราะโดนเตะจาก login ซ้ำ
แก้: แยก 2 แบบใน SessionWatcher
- activityCheck (โหลดหน้า/รีเฟรช/สลับกลับมาที่แท็บ) → ยิง /users/me ที่ bump lastSeenAt
  = รีเซ็ตนาฬิกา idle → รีเฟรชไม่หมดอายุ (ตราบใดที่ยังไม่โดนเตะ/ไม่ idle เกิน 30 นาที)
- passiveCheck (poll ทุก 15 วิ ระหว่างเปิดค้าง) → /auth/session ไม่ bump → ปล่อย idle เดิน
  ตามจริง แต่ยังจับได้ว่าโดนเตะ/หมดอายุเพื่อเด้งออก
สรุปพฤติกรรม: รีเฟรช/กลับมาที่แท็บ = อยู่ต่อ · ไม่แตะ 30 นาที = หลุด · login ที่อื่น = เตะตัวเก่า
tsc ผ่าน 0 error

[65] ระบบไม่เก็บค่ามัดจำ (ผู้เช่าจ่ายค่าห้องอย่างเดียว) + สูตรค่าคอม/ส่วนแบ่งเดิมคิดผิดฐาน
--------------------------------------------------------
ไฟล์: packages/shared/src/logic/commission.ts, constants/fees.ts, types/booking.ts,
apps/api/prisma/schema.prisma (migration add_booking_price_deposit),
apps/api/src/modules/bookings/bookings.service.ts (create),
apps/api/src/modules/payments/payments.service.ts,
apps/api/src/modules/admin/finance/finance.service.ts (listDetailed, getOwnerDetail),
apps/web: bookings/new, bookings/[id], bookings/[id]/pay, admin/finance,
admin/finance/owners/[ownerId], partner/dashboard
อาการ (เจ้าของระบบรายงาน): (1) ตอนจอง booking.amount = room.pricePerMonth อย่างเดียว
ค่ามัดจำไม่ถูกเก็บผ่านระบบเลย → ผู้เช่าต้องไปจ่ายมัดจำกับเจ้าของหอเองนอกระบบ = เสี่ยง
โดนโกง (2) หน้าจองโชว์มัดจำเป็น "ชำระกับหอโดยตรง" และยอดจ่ายรวมไม่รวมมัดจำ
คำสั่งเจ้าของระบบ + สูตรที่ตกลง:
- amount = ค่าห้อง + มัดจำ (เก็บรวมกับ Hopak ทั้งก้อน กันโกง) เช่น 1800+2000=3800
- ค่าคอม 20% คิดจาก "ค่าห้อง" เท่านั้น (มัดจำไม่โดนหัก ส่งเจ้าของเต็ม)
- หอการค้า (chamberShare) = 10% ของ "ค่าคอม" (ไม่ใช่ 10% ของยอดเต็มแบบเดิม)
  เช่น ค่าห้อง 100 → คอม 20 → หอการค้า 2 ; แพลตฟอร์ม = 90% ของคอม = 18
- เจ้าของหอได้ = (ค่าห้อง − คอม) + มัดจำเต็ม เช่น (1800−360)+2000 = 3440
แก้ไข:
- schema: Booking เพิ่ม roomPrice, deposit (snapshot ตอนจอง กันราคาห้องเปลี่ยนภายหลัง)
  migrate + backfill roomPrice=amount ให้ booking เก่า (deposit คง 0 เพราะเดิมไม่เก็บ)
- commission.ts: calcCommission(rent)=20%rent; calcChamberShare=10%ของคอม;
  calcPlatformShare=คอม−chamber; calcOwnerPayout(rent,deposit)=rent−คอม+deposit
- bookings.create: roomPrice=pricePerMonth, deposit=ห้อง||หอ, amount=รวม
- payments: คิด commission/chamber/platform จาก booking.roomPrice, payout รวมมัดจำ
- finance.service: listDetailed/getOwnerDetail คืน roomPrice+deposit เพิ่ม
- web tenant: หน้าจองโชว์ยอดรวม (ค่าห้าง+มัดจำ) + มัดจำเปลี่ยนข้อความเป็น "ชำระผ่าน
  Hopak พร้อมค่าเช่า (กันโอนตรงเจ้าของหอ)"; ใบเสร็จ/หน้าจ่ายเงินแยกโชว์ค่าห้อง/มัดจำ
- web admin finance + owner detail: เพิ่มคอลัมน์ ค่าห้อง/มัดจำ + แก้ label %
  ที่ไม่ตรง (เดิม "หอการค้า 10%/แพลตฟอร์ม 10%/โอนเจ้าของ 80%")
- web partner/dashboard: จุดสำคัญ — เดิม calcOwnerPayout(รวมยอดทั้งเดือน) จะหักคอม
  จากมัดจำผิด แก้เป็นคิด payout ต่อ booking จาก roomPrice/deposit แยก แล้วรวม
สถานะ: แก้แล้ว migration apply + backfill สำเร็จ, ตรวจตัวเลขด้วย node ตรงเป๊ะ
(100→คอม20→หอการค้า2; 1800+2000→คอม360 chamber36 platform324 payout3440)
tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (29/07/2026)
หมายเหตุ: มัดจำเป็นเงินประกันของผู้เช่า — ตอนนี้ Hopak เก็บแล้วส่งต่อเจ้าของหอเต็ม
(เจ้าของหอถือไว้คืนผู้เช่าตอนย้ายออกเอง) ระบบยังไม่มี flow "คืนมัดจำ" อัตโนมัติ
(ถ้าต้องการให้ Hopak เป็นคนถือ/คืนมัดจำแทน ค่อยทำเพิ่มทีหลัง)

[65 แก้ไขเพิ่ม 29/07/2026] เจ้าของระบบเปลี่ยนใจ: คอม 20% คิดจาก "ยอดรวม" (ค่าห้อง+มัดจำ)
ไม่ใช่ค่าห้องอย่างเดียว — มัดจำโดนหักคอมด้วย
- commission.ts กลับมารับ arg เดียว (ยอดรวม): calcCommission(amount)=20%amount;
  chamber=10%ของคอม; platform=90%ของคอม; ownerPayout(amount)=amount−คอม (80%)
- payments.service: คิดทุกอย่างจาก booking.amount (ยอดรวม) ไม่ใช่ roomPrice
- partner/dashboard: monthlyNet = calcOwnerPayout(ยอดรวมทั้งเดือน) (80% ของยอดรวม)
- roomPrice/deposit ยังเก็บ+โชว์แยกในแดชบอร์ดเหมือนเดิม (แค่ไม่ใช่ฐานคิดคอมแล้ว)
- label finance กลับเป็น "คอม 20% / โอนเจ้าของ 80%" (ตรงกับยอดรวม)
ตัวเลขใหม่ ยอดรวม 3800: คอม 760, หอการค้า 76, แพลตฟอร์ม 684, เจ้าของได้ 3040
tsc ผ่าน 0 error ทั้ง 2 apps

[65 แก้ไขเพิ่ม 2 29/07/2026] เจ้าของระบบสั่ง: ปิดส่วนแบ่งหอการค้าไว้ก่อน หัก 20% เพียวๆ
- fees.ts: CHAMBER_RATE = 0 (เดิม 0.1) → chamberShare=0, platformShare=คอมเต็ม
  เปิดกลับเมื่อพร้อมแค่ตั้ง 0.1 (โครงสร้าง+field เก็บครบ ไม่ต้องแก้โค้ดอื่น)
- web finance + owner detail: เอาการ์ดสรุป "หอการค้ามหาสารคาม" + คอลัมน์หอการค้าออก
  (grid 4→3 คอลัมน์) label แพลตฟอร์ม = "รายได้แพลตฟอร์ม (คอม 20%)"
สรุปสูตรสุดท้าย: ผู้เช่าจ่าย ค่าห้อง+มัดจำ → หัก 20% เข้าแพลตฟอร์ม → เจ้าของได้ 80%

[66] เพิ่มหน้า admin ประวัติการเข้าสู่ระบบ (IP + บราวเซอร์ที่ใช้)
--------------------------------------------------------
ไฟล์: apps/api/src/modules/admin/users/{admin-users.service,admin-users.controller}.ts,
apps/web/src/app/(admin)/admin/sessions/page.tsx (ใหม่),
apps/web/src/app/(admin)/admin/layout.tsx (nav + title map)
คำขอ: หน้า admin เก็บ/ดูประวัติการเข้าใช้งาน พร้อม IP และบราวเซอร์
ทำ: Session model เก็บ ip/userAgent/createdAt/lastSeenAt/revokedAt อยู่แล้ว (ไม่ต้อง
migrate) — เพิ่ม
- backend GET /admin/users/sessions (admin only, วางก่อน :id routes) คืน 200 รายการ
  ล่าสุด: ชื่อ/อีเมล/บทบาทผู้ใช้ + ip + userAgent + loginAt + lastSeenAt + active
- frontend หน้า /admin/sessions: ตารางผู้ใช้/บทบาท/IP/บราวเซอร์(parse ua เป็น
  Chrome·Windows ฯลฯ ไม่ใช้ไลบรารี)/เข้าเมื่อ/ใช้ล่าสุด/สถานะ(ใช้งานอยู่·สิ้นสุด)
  + ปุ่มกรอง "เฉพาะที่ใช้งานอยู่" + เมนู sidebar + title
สถานะ: แก้แล้ว ทดสอบ endpoint คืน 17 sessions จริง tsc ผ่าน 0 error ทั้ง 2 apps
หมายเหตุ: ผูกกับ [64] — active=session ที่ยังไม่ revoke (เห็นได้ว่าใครโดนเตะ/idle ไปแล้ว)

[66 เพิ่ม 29/07/2026] ดูประวัติย้อนหลังทีละเดือน (เก็บเป็นเดือนๆ)
- backend: listSessions(year?,month?) filter ตาม createdAt range +
  GET /admin/users/sessions/periods คืนเดือน/ปีที่มีข้อมูล (วางก่อน route sessions)
  ไม่เลือกเดือน=200 ล่าสุด · เลือกเดือน=ทุกรายการเดือนนั้น (take 1000)
- frontend หน้า /admin/sessions: dropdown เลือกเดือน (ทุกเดือน + รายเดือนจาก periods)
  ดูย้อนหลังได้ตลอด — เปลี่ยนเดือนแล้ว refetch
ทดสอบ: periods=[ก.ค.2026], กรองเดือน 7/2026 คืน 19 รายการ tsc ผ่าน 0 error

[67] ระบบตัดห้อง: ย้ายจากเจ้าของหอ (เสี่ยงทุจริต) มาให้ระบบ/แอดมินคุม
--------------------------------------------------------
ไฟล์: apps/api/src/modules/admin/finance/finance.service.ts (settlePayment),
apps/api/src/modules/bookings/bookings.service.ts (adminCancel),
apps/api/src/modules/rooms/{rooms.service,rooms.controller}.ts (setStatus),
apps/web: (partner)/partner/rooms/page.tsx, (admin)/admin/bookings/page.tsx
คำขอ: เดิมเจ้าของหอกดตัดห้องเอง = เสี่ยงทุจริต. เปลี่ยนเป็น:
- แอดมินเคลียร์บิล (แมนนวล เช็คบัญชีแล้วกดเอง) → ระบบตัดห้องอัตโนมัติ + ออกโทเค็น
- แอดมินตัด/คืนห้องเองได้ต่อห้อง · คืนห้อง = ยกเลิกการจอง (คืนเงินเบื้องหลัง)
- เอาสิทธิ์ตัดห้องออกจากเจ้าของหอ
แก้:
- settlePayment: หลัง booking PAID → room = OCCUPIED อัตโนมัติ (โชว์ไม่ว่างบนเว็บทันที)
  เดิม markPaid() ที่ตัดห้องไม่มีใครเรียก = ห้องค้างว่างทั้งที่จองแล้ว
- adminCancel: override (ไม่เช็ค state machine) ยกเลิกได้ทุกสถานะรวม PAID +
  $transaction booking CANCELLED + room AVAILABLE + แจ้งผู้เช่า = "คืนห้อง/คืนเงิน"
- rooms setStatus: @Roles('owner')→'admin', service ตัด ownership check (แอดมินคุม)
- partner/rooms: เอาปุ่มตัด/คืนห้องออก เหลือ badge สถานะ read-only (จัดการโดยระบบ)
- admin/bookings: เพิ่มคอลัมน์ "ห้อง" (badge ว่าง/ไม่ว่าง + ปุ่มตัด/คืน) +
  ปุ่ม "คืนห้อง (คืนเงิน)" สำหรับ booking PAID (confirm ก่อน) เรียก admin-cancel
สถานะ: แก้แล้ว tsc --noEmit ผ่าน 0 error ทั้ง 2 apps (29/07/2026)
Decisions เจ้าของระบบ: คืนห้อง=ยกเลิก+ห้องว่าง · เอาปุ่มตัดห้องเจ้าของออก
หมายเหตุ: การคืนเงินจริงทำเบื้องหลัง (ยังไม่มี flow refund อัตโนมัติในระบบ)

[68] หน้า booking flow ขั้นตอน 2-5 UI พื้นๆ ไม่ตรงดีไซน์ (Hoprak User Booking.dc.html)
--------------------------------------------------------
ไฟล์: apps/web/src/components/booking/{BookingStepper,BookingSummary}.tsx (ใหม่),
apps/web/src/app/(tenant)/bookings/[id]/page.tsx, bookings/[id]/pay/page.tsx
อาการ: ดีไซน์เป็น flow ขั้นตอนเดียว — stepper แนวนอน 5 ขั้น + panel ซ้ายเปลี่ยนตามขั้น +
sidebar สรุปขวา. หน้า /new (step1) ทำตรงแล้ว แต่ step 2-5 (detail + pay) ยังเป็น UI พื้น
(stepper เป็นลิสต์ข้อความ, หน้า pay เป็นกล่อง QR เปล่าๆ, ไม่มีการ์ด waiting/confirmed)
แก้:
- BookingStepper: stepper แนวนอน 5 ขั้น (done เขียว/active น้ำเงิน/upcoming เทา) ใช้ร่วมทุกหน้า
- BookingSummary: sidebar สรุป (รูป+เรตติ้ง+ชื่อหอ + ค่าเช่า/มัดจำ/ยอดรวม + reassurance)
  รับ children เป็นปุ่ม CTA เฉพาะแต่ละขั้น
- bookings/[id]: รื้อ layout ตามดีไซน์ (header back+title+sub, stepper, grid 2 คอลัมน์)
  panel ซ้ายตามสถานะ: pending=การ์ดรอยืนยัน(spinner+เตือน), confirmed=การ์ดยืนยัน+สรุป,
  paid/completed=ใบเสร็จ+โทเค็น(ของเดิม), cancelled=การ์ดยกเลิก. CTA ใน sidebar ตามสถานะ
- bookings/[id]/pay (step4): รื้อใหม่ — การ์ดบัญชีโอน(คัดลอกเลขบัญชี) + dropzone แนบสลิป
  (พรีวิวรูป) + stepper + sidebar สรุป. คงตรรกะอัปโหลดสลิปเดิม (FormData → /payment)
stepper map: pending→2 confirmed→3 pay→4 paid/completed→5
สถานะ: แก้แล้ว tsc ผ่าน 0 error, route /bookings/:id + /pay คืน 200 (ไม่ 500)
(29/07/2026) ยังไม่ได้ E2E เบราว์เซอร์จริง — ผู้ใช้เทสด้วยการจองจริงได้เลย

[69] Google login พัง "google_login_failed" — fragment (#token) หายตอน 302 redirect ข้าม origin
--------------------------------------------------------
ไฟล์: apps/api/src/modules/auth/{auth.service,auth.controller}.ts,
apps/web/src/app/(auth)/auth/google/callback/page.tsx
อาการ: login ด้วย Google เด้งกลับ /login?error=google_login_failed ทุกครั้ง
ต้นตอ: การแก้ [63] เปลี่ยน redirect จาก ?token= เป็น #token= (กัน JWT รั่ว log/Referer)
แต่ URL fragment (#) "ไม่รอด" การ 302 redirect ข้าม origin (:4000 API → :3001 web) —
เบราว์เซอร์ทิ้ง fragment ทำให้หน้า callback อ่าน token ไม่เจอ เข้าเงื่อนไข else → error
ยืนยันด้วย headless: ใส่ #token= ตรงๆ อ่านได้ (hasToken true) แต่ผ่าน redirect จริงหาย
แก้ (one-time exchange code — ปลอดภัย + รอด redirect):
- auth.service: googleCodes Map เก็บ {code -> token, หมดอายุ 2 นาที} + createGoogleExchangeCode()
  + exchangeGoogleCode() (ใช้ครั้งเดียว ลบทันที + กวาดหมดอายุ)
- auth.controller: callback สร้าง code แล้ว redirect ?code=<code> (query รอด redirect),
  เพิ่ม POST /auth/google/exchange (@RateLimit 20/นาที) แลก code -> {accessToken}
- web callback: อ่าน ?code จาก query แล้ว POST แลก JWT → setToken → window.location.replace('/')
  (hard nav กัน Next router ค้างหลังเปลี่ยน URL — เจอบั๊กรองว่า router.replace ไม่ navigate)
เหตุผลปลอดภัย: code หลุด log ก็ไร้ค่า (ใช้ครั้งเดียว/2 นาที) ต่างจาก JWT 7 วันที่หลุดแล้วเข้าบัญชีได้
สถานะ: แก้แล้ว tsc ผ่าน 0 error ทั้ง 2 apps, ทดสอบ headless: code ปลอม→เด้ง error ถูก,
exchange bad code→401 (29/07/2026)
หมายเหตุ: prod ก็มีบั๊กนี้จาก [63] เหมือนกัน — ต้อง deploy fix นี้ด้วยตอนขึ้น prod

[70] หน้า pay ดีดกลับ step 3 หลังจ่าย + ขาด state "รอแอดมินตรวจสลิป"
--------------------------------------------------------
ไฟล์: apps/api/src/modules/bookings/bookings.service.ts (findOne include payment),
packages/shared/src/types/booking.ts (payment field),
apps/web: bookings/[id]/page.tsx, bookings/[id]/pay/page.tsx
อาการ: หลังผู้เช่าจ่าย/แนบสลิป → payment=PENDING แต่ booking ยัง CONFIRMED
(รอแอดมินเคลียร์บิล) → หน้า detail โชว์ step 3 (confirmed) พร้อมปุ่มจ่ายอีก = เหมือนถอย
กลับ. หน้า pay ก็เข้าซ้ำได้ทั้งที่จ่ายแล้ว
แก้:
- findOne include payment.status → frontend รู้ว่าจ่าย/แนบสลิปแล้วหรือยัง
- Booking type เพิ่ม payment?: {status}
- detail: เพิ่ม state awaitingVerify (confirmed + มี payment) = step 4 การ์ด "แนบสลิป
  เรียบร้อย · รอแอดมินตรวจสอบ" (spinner) + ปุ่มเทา "รอแอดมินตรวจสลิป" (ไม่โชว์ปุ่มจ่ายซ้ำ)
  แยก canPay (confirmed + ยังไม่จ่าย) = step 3 โชว์ปุ่มจ่าย
- pay page guard: จ่ายได้เฉพาะ CONFIRMED + ยังไม่มี payment, สถานะอื่นเด้งกลับ detail
  ใช้ window.location.replace (hard nav) — router.replace บางครั้งไม่ navigate ใน App Router
สถานะ: แก้แล้ว tsc ผ่าน 0 error, ถ่ายจอยืนยัน state รอตรวจสลิป render ถูก (step 4)
หมายเหตุ: หน้า detail มี socket booking:updated refetch อยู่แล้ว → แอดมินเคลียร์บิล
หน้าเด้งเป็น step 5 (ใบเสร็จ) เองเรียลไทม์ ไม่ต้องรีเฟรช

[71] แจ้งเตือนเรียลไทม์: กระดิ่งเด้ง + toast แดง ตอนมี notification ใหม่
--------------------------------------------------------
ไฟล์: apps/web/src/components/Navbar.tsx
คำขอ: มี notification ให้วิ่งเข้ากระดิ่ง + toast แดงเรียลไทม์ ไม่ต้องรีเฟรช
ทำ: socket 'notification:new' (มีอยู่แล้วจาก [60]) เดิมแค่เพิ่มตัวนับ — เพิ่ม:
- กระดิ่ง ping ring แดง + icon เด้ง (animate-bounce) 2.5 วิ เมื่อมีใหม่
- toast แดงมุมขวาบน (title+body ของ notification) กดไปหน้าแจ้งเตือน หายเองใน 6 วิ
สถานะ: แก้แล้ว tsc ผ่าน 0 error (30/07/2026)

[72] อัฟเดท UI หน้าแรก (/) ให้ตรงดีไซน์ Hopak Web Home (DesignSync)
ไฟล์: apps/web/src/app/page.tsx
- Hero: gradient ฟ้าสว่าง linear-gradient(120deg,#2F6FE0,#2456B8) แทนกรมท่าเข้ม
- การ์ดค้นหาลอยทับ hero (-mt-120/140px, z-2) ขอบฟ้า border-tenant แทนขอบส้ม
- แท็บ หอพักรายเดือน/รายวัน มุมโค้งบน ต่อกับการ์ด
- โครงในการ์ด: ช่องค้นหาใหญ่เต็มบน + ประเภทห้อง/งบประมาณ 2 คอลัมน์ + ปุ่มค้นหาเต็มความกว้าง
- ชิปยอดนิยมย้ายมาใต้การ์ด (พื้นขาว border เทา) แทนชิปโปร่งบน hero
- Footer: เปลี่ยนพื้นดำเป็นขาว 5 คอลัมน์ + แถบดำล่าง (โลโก้+copyright)
- คง logic เดิมครบ: ค้นหาจริง, Google Places autocomplete, i18n TH/EN, favorites, กระดิ่งแจ้งเตือน realtime, ข้อมูลหอจริง
สถานะ: แก้แล้ว tsc ผ่าน 0 error ยืนยันด้วย screenshot (30/07/2026)

[73] เพิ่มหน้าแอดมิน "แก้ไขหน้าแรก (inline)" — แก้ข้อความหน้าแรกแบบคลิกพิมพ์ทันที (DesignSync: Hopak Admin Inline Edit)
Backend:
- schema SiteSettings + homeContent Json @default("{}") (migration add_home_content)
- settings.service: setHomeContent (merge/trim, ช่องว่าง=ลบ override), clearHero (เอารูป hero ออก), getHero คืน homeContent
- settings.controller: POST /admin/settings/home-content, DELETE /admin/settings/hero (admin only)
Frontend:
- หน้าใหม่ apps/web/src/app/(admin)/admin/website/edit/page.tsx: sticky save bar + ตัวนับจุดที่แก้ (เทา/เขียว), hint strip, live preview แก้ inline (contentEditable) ของ hero title/subtitle, การ์ดจุดขาย 3 ใบ (tag/title/sub), หัวข้อ+คำโปรย "ทำเลยอดนิยม", trust cards 3 ใบ
- เปลี่ยน/เอารูปพื้นหลัง hero (อัปโหลด/ลบ บันทึกทันที) ปุ่มมุมขวาบน
- ปุ่มยกเลิก (remount คืนค่าเดิม) + บันทึก (POST home-content + promos) + toast
- แก้เฉพาะภาษาไทย (อังกฤษใช้ default) ตามดีไซน์
- หน้าแรก page.tsx: consume homeContent ทับ default เมื่อดูภาษาไทย (heroTitle/subtitle, zonesTitle/sub, trust 3 ใบ) ช่องว่าง=ใช้ default
- เพิ่มเมนู sidebar แอดมิน "แก้ไขหน้าแรก (inline)" / "Edit Homepage (inline)"
สถานะ: แก้แล้ว tsc ผ่าน 0 error ทั้ง api+web, migration ลงแล้ว, /settings/hero คืน homeContent, หน้าแรก render ปกติ (30/07/2026)

[74] อัปโหลดรูปพื้นหลัง hero แล้วหน้าแรกไม่เปลี่ยน
สาเหตุ: heroImageUrl เก็บใน DB ถูกต้อง (POST /admin/settings/hero คืน 201 เปลี่ยน URL จริง) แต่หน้าแรก apps/web/src/app/page.tsx hero hardcode เป็น gradient ฟ้าตายตัว ไม่เคย consume heroImageUrl
แก้: page.tsx เพิ่ม state heroImageUrl (ดึงจาก /settings/hero), hero div ใช้รูปเป็น background (bg-cover/center) + overlay เข้ม rgba(20,40,90,.62/.42) ให้ตัวอักษรขาวอ่านออก; ไม่มีรูป = gradient เดิม
สถานะ: แก้แล้ว tsc ผ่าน ยืนยันด้วย screenshot (หน้าแรกโชว์รูป hero ที่อัปโหลด) — เทส upload backend คืน 201 เปลี่ยน URL จริง (30/07/2026)

[75] hero: เอา overlay/ฟิลเตอร์ออกให้เห็นภาพจริง + เลือกสีพื้นหลังตอนไม่มีรูป
- เอา overlay เข้มบนรูป hero ออกทั้งหน้าแรก + หน้า editor → เห็นภาพจริงชัด ใส่ text-shadow แทนให้ตัวอักษรขาวยังอ่านออก
- ตอนไม่มีรูป: เลือกสีพื้นหลัง hero ได้จาก palette 8 สี (ฟ้าเริ่มต้น=gradient / ฟ้าเข้ม / กรมท่า / เขียว / ส้ม / ม่วง / แดง / ชมพู)
Backend: HomeContent + heroColor (validate ^#[0-9a-fA-F]{3,8}$ กันค่าขยะ, ว่าง=ลบ)
Frontend หน้าแรก: hero ใช้ heroColor เป็นพื้นหลังตอนไม่มีรูป, มีรูป=ภาพจริงไม่มี filter + text-shadow
Frontend editor: palette วงกลมสีใต้ hero (โชว์เฉพาะตอนไม่มีรูป) กดเลือก=dirty บันทึกลง home-content
สถานะ: แก้แล้ว tsc ผ่าน api+web, เทส heroColor: hex ผ่าน/ค่าขยะถูกปฏิเสธ/ว่างล้าง, screenshot หน้าแรกภาพ hero ไม่มี filter แล้ว (30/07/2026)

[76] hero: เลือกตำแหน่งรูป (background-position) พรีวิวสดก่อนบันทึก
Backend: HomeContent + heroPos ("X% Y%") validate ^\d{1,3}% \d{1,3}%$ (ค่าขยะ/ผิดรูป=ไม่เก็บ ใช้ center)
Frontend หน้าแรก: hero backgroundPosition = homeContent.heroPos || center (เฉพาะตอนมีรูป)
Frontend editor: ตอนมีรูปโชว์ slider 2 อัน (แนวนอน/แนวตั้ง 0-100%) + ปุ่มรีเซ็ตกึ่งกลาง เลื่อน=พรีวิวสด, dirty, บันทึกลง home-content
สถานะ: แก้แล้ว tsc ผ่าน api+web, เทส heroPos: ค่าถูกเก็บ/ค่าขยะปฏิเสธ, screenshot ยืนยันรูปเลื่อนตำแหน่งตาม % จริง (30/07/2026)

[77] ปฏิเสธหอพัก (register seller): เดิม reject แค่ตั้ง REJECTED ไม่มีเหตุผล ไม่แจ้งเตือน เจ้าของหอแก้/ส่งใหม่ไม่ได้ (ค้าง) — ทำระบบใหม่
Schema: Dorm + rejectionReason String?, rejectionCount Int @default(0) (migration dorm_rejection_reason)
Backend:
- ApprovalsService.reject(dormId, adminId, reason): บังคับมีเหตุผล (ว่าง=400), ตั้ง REJECTED + เก็บ reason + rejectionCount+1, แจ้งเตือนเจ้าของหอ in-app (type DORM_REJECTED, realtime) + อีเมล (เหตุผล+วิธีแก้), ครบ 3 ครั้งข้อความเตือนว่าอาจถูกระงับ
- approve() ล้าง rejectionReason
- DormsService.resubmit(ownerId, id): หอที่ REJECTED → PENDING_APPROVAL + ล้าง reason (คง count เป็นประวัติ), เช็ค ownership + สถานะ
- routes: PATCH /admin/approvals/:dormId/reject (body reason), PATCH /dorms/:id/resubmit (owner)
Frontend:
- admin approvals: กดปฏิเสธ → prompt กรอกเหตุผล+วิธีแก้ (ยกเลิก=ไม่ปฏิเสธ), โชว์ "ถูกปฏิเสธ N ครั้ง" + เหตุผลครั้งก่อน, ครบ 3 ครั้งขึ้นปุ่ม "ระงับหอนี้ทันที"
- partner dashboard: แบนเนอร์แดงหอที่ถูกปฏิเสธ + เหตุผล/วิธีแก้ + ปุ่มแก้ไขข้อมูล + ส่งอนุมัติใหม่
- shared Dorm type + rejectionReason/rejectionCount
เทส (owner@hopak.com + admin): reason ว่าง=400, reject=REJECTED count1 reason เก็บ, owner ได้ notif DORM_REJECTED, resubmit=PENDING_APPROVAL reason=null, reject 3 ครั้ง count=3
หมายเหตุ: ไม่ได้ทำ owner→user ปกติ ตอน reject (ขัดกับ "แก้แล้วส่งใหม่" ที่ต้องคง owner) — คงบัญชี owner ไว้ให้แก้+ส่งกลับ รอยืนยันถ้ายังอยากให้ downgrade
สถานะ: แก้แล้ว tsc ผ่าน api+web, เทส flow ครบผ่าน (30/07/2026)

[77b] ปรับเพิ่ม: reject หอ → ลดสิทธิ์เจ้าของหอกลับเป็น user ปกติ (TENANT) + คืนสิทธิ์ตอนอนุมัติ
- ApprovalsService.reject: หลังตั้ง REJECTED ถ้าเจ้าของ "ไม่มีหออื่นที่ APPROVED" → ลด role OWNER→TENANT (กันเจ้าของหลายหอเสียสิทธิ์หอที่ดี), แจ้งเตือนบอกให้ขอเป็นเจ้าของหอใหม่
- ApprovalsService.approve: คืน role → OWNER (เผื่อโดนลดตอนถูกปฏิเสธก่อนหน้า)
- flow ใหม่: reject → เป็น user ปกติ → ขอเป็นเจ้าของหออีกครั้ง (become-owner) → แอดมินอนุมัติ = คืน OWNER → แก้หอ+ส่งอนุมัติใหม่ ; ครบ 3 ครั้งระงับได้
เทส: owner สร้างใหม่ (หอเดียว) reject→role TENANT, approve→role OWNER ; owner@hopak.com (มีหอ APPROVED อื่น) reject→ยังเป็น OWNER (guard ทำงาน)
หมายเหตุ: token เดิมยัง cache role OWNER จนกว่า re-login (role อยู่ใน JWT) — ผลเต็มตอนล็อกอินใหม่
สถานะ: แก้แล้ว tsc api ผ่าน เทส demote/restore ผ่าน ลบ test data แล้ว (30/07/2026)

[78] ขอเป็นเจ้าของหอ (become-owner) กดครั้งเดียว = คำขอเปล่า admin ไม่มีข้อมูล/เอกสารตรวจ
Schema: OwnerRequest + phone, dormName, province, address, note, documents String[] (migration owner_request_details)
Backend:
- UsersService.requestOwner(userId, dto, files): บังคับกรอก dormName+province+phone + แนบเอกสาร >=1 (ว่าง=400), อัปโหลดเอกสารแบบ private, สร้าง OwnerRequest พร้อมข้อมูล; ถ้ามีคำขอ pending อยู่แล้ว=409
- myOwnerRequest + admin listPending: แปลง documents (storage key) เป็น signed URL
- controller become-owner: FilesInterceptor('documents',6) multipart
- OwnerRequestsService inject UploadsService
Frontend:
- profile OwnerPitch: เปลี่ยนปุ่มกดครั้งเดียว เป็นฟอร์ม (ชื่อหอ/จังหวัด(select)/เบอร์/ที่อยู่/หมายเหตุ + อัปโหลดเอกสารหลายไฟล์) submit multipart, validate ก่อนส่ง
- admin owner-requests page: โชว์ ชื่อหอ/จังหวัด/ที่อยู่/หมายเหตุ + ลิงก์เอกสาร
- shared OwnerRequest type + ฟิลด์ใหม่
เทส (tenant ใหม่ + admin): empty=400, submit(fields+doc)=201 เก็บครบ, myOwnerRequest คืน signed URL, admin เห็นข้อมูล+เอกสาร signed
สถานะ: แก้แล้ว tsc api+web ผ่าน migration ลงแล้ว เทส flow ผ่าน ลบ test data (30/07/2026)

[79] become-owner (/profile "ขอเป็นเจ้าของหอ") เปลี่ยนจากฟอร์มเดียว เป็น wizard ทีละขั้น (เหมือน /partner-register)
- profile OwnerPitch: stepper 3 ขั้น
  1) ข้อมูลหอพัก (ชื่อหอ/จังหวัด/เบอร์/ที่อยู่/หมายเหตุ) — validate ก่อนไปต่อ
  2) เอกสารยืนยัน (อัปโหลดหลายไฟล์) — บังคับ >=1 ก่อนไปต่อ
  3) ตรวจสอบและส่ง (สรุปข้อมูล + ปุ่มส่งคำขอ)
- ปุ่ม ถัดไป/ย้อนกลับ/ยกเลิก, แถบ step 1→2→3, backend เดิม (POST multipart /users/me/become-owner)
- verify: screenshot wizard ขั้น 1 render ครบ (stepper + fields + ถัดไป/ยกเลิก), tsc web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)

หมายเหตุ: ระหว่างงานนี้ API dev process ดับชั่วคราว (client เจอ "Failed to fetch") — restart pnpm dev แล้ว api+web=200 กลับปกติ ไม่ใช่บัคโค้ด

[80] เอาการ์ด "เปิดหอพักกับ Hoprak / ขอเป็นเจ้าของหอ" (OwnerPitch wizard) ออกจาก /profile ตามที่ผู้ใช้ขอ
- ลบ render {isTenant && <OwnerPitch/>} + ลบ component OwnerPitch ทั้งก้อน + ลบ import PROVINCES/OwnerRequest ที่ไม่ใช้
- backend become-owner (endpoint + wizard logic) ยังคงอยู่ เผื่อเปิดใช้ทีหลัง
- tsc web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)

[81] แดชบอร์ดเจ้าของหอ: เพิ่มการ์ด "สรุปรายได้เดือนนี้ (แยกรายละเอียด)"
- แยก ค่าห้อง (เต็ม) / ค่ามัดจำ (เต็ม) / ยอดเต็มเดือนนี้ (room+deposit)
- แถวสรุป: ยอดเต็ม → หักค่าคอมมิชชัน 20% (−) → คงเหลือสุทธิ (80%)
- โบนัส: ป้าย "เร็วๆ นี้" (ยังไม่ทำระบบ)
- คำนวณจาก paid/completed bookings เดือนปัจจุบัน (b.roomPrice, b.deposit) ใช้ calcCommission/calcOwnerPayout
- ไฟล์: apps/web/src/app/(partner)/partner/dashboard/page.tsx (TEXT th/en + การ์ดหลัง grid รายได้/occupancy)
- verify: tsc web ผ่าน; ข้อมูลจริง owner@hopak.com เดือนนี้ ค่าห้อง3500/มัดจำ0/เต็ม3500/หัก700/สุทธิ2800 ถูกต้อง
สถานะ: แก้แล้ว (30/07/2026)

[82] /partner/settings: เพิ่มช่องกรอกเบอร์ติดต่อเจ้าของหอพัก
- การ์ด "ข้อมูลเจ้าของหอพัก" เพิ่ม input เบอร์โทร (แก้ได้) โหลดจาก user.phone
- handleSave เดิม PATCH /users/me เพิ่ม field phone (บันทึกพร้อมข้อมูลรับเงินด้วยปุ่มเดียว)
- backend รองรับอยู่แล้ว (UpdateProfileDto.phone)
- TEXT th/en (ownerPhone/placeholder)
- เทส: PATCH /users/me phone null→0891234567→คืนค่า ผ่าน; tsc web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)

[83] หน้าโอนเงินแอดมิน (finance/payouts) โชว์เบอร์เจ้าของหอ เผื่อติดต่อกลับ
- finance.service listPendingPayouts: เพิ่ม ownerPhone: dorm.owner.phone
- admin finance page: PendingPayout + ownerPhone; โชว์ในการ์ดหอ (📞 ข้างชื่อเจ้าของ) + ในโมดัลยืนยันโอนเงิน
- เทส: PATCH เบอร์เจ้าของ → GET /admin/finance/payouts คืน ownerPhone ถูกต้อง; tsc api+web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)

[84] แดชบอร์ดเจ้าของหอ: การ์ด "รายได้จากการโอน" (รอโอน/โอนแล้ว) อัปเดตอัตโนมัติ (DesignSync Hopak Partner Panel)
Backend:
- PaymentsService.getOwnerIncome(ownerId): payments SETTLED=รอโอน, TRANSFERRED=โอนแล้ว ของหอตัวเอง; คืน received/pending + rows (ref,ผู้เช่า,payout,status,transferSlipUrl signed)
- OwnerIncomeController ใหม่ GET /partner/income (Roles owner) + ลงทะเบียนใน PaymentsModule
Frontend dashboard:
- การ์ด 2 ช่อง: โอนแล้ว (เขียว) / รอโอนจากแอดมิน (ส้ม) + รายการล่าสุดพร้อม badge สถานะ + ลิงก์ดูสลิป
- realtime: subscribe 'notification:new' → refetch /partner/income (แอดมินโอน→เด้งเป็นโอนแล้วอัตโนมัติ)
เทส: GET /partner/income owner@hopak.com = received 9550 / pending 0 / 5 rows (row0 transferred+slip) ; tsc api+web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)

หมายเหตุ: API dev process ล้มซ้ำๆ ตอน nest watch recompile/restart (client เจอ api:000 "Failed to fetch") — restart pnpm dev แล้วหาย ไม่ใช่บัคโค้ด (tsc ผ่าน endpoint ทำงานเมื่อ api ขึ้น)

[85] จัด partner panel ให้ตรงดีไซน์ Hopak Partner Panel (แยกหน้า income + CTA + nav)
- หน้าใหม่ /partner/income (apps/web/.../partner/income/page.tsx) ตามดีไซน์:
  · การ์ดดำ "ค่าห้องที่ได้รับแล้ว" (received, เขียว) + การ์ดส้ม "รอโอนจากแอดมิน" (pending)
  · ตาราง "ค่าห้องแยกตามการจอง": เลขที่/ผู้เช่า/ห้อง/ค่าห้องสุทธิ/สถานะ (โอนแล้ว✓/รอโอน + ดูสลิป) + แถวรวม + note
  · realtime notification:new → refetch (แอดมินโอน→เด้งโอนแล้วอัตโนมัติ)
- sidebar partner: เพิ่มเมนู "รายได้จากแอป" (/partner/income) ตำแหน่งที่ 2 + PAGE_HEADER
- dashboard: ลบการ์ด income[84] + breakdown[81] ที่ยัดเกิน (ไม่มีในดีไซน์) → ใส่ CTA การ์ดดำ "รายได้จากแอป Hoprak" ลิงก์ /partner/income (ตามดีไซน์) ; คง stats/revenue/occupancy/recent/todo
- ลบ dead code (income state/effect/interfaces/getSocket/breakdown compute/calcCommission) ใน dashboard
เทส: endpoint /partner/income คืนข้อมูลจริง; tsc web ผ่าน (screenshot partner route ติด headless-auth เตะ login — ไม่ใช่บัคหน้า)
สถานะ: แก้แล้ว (30/07/2026)

[86] หน้าแจ้งเตือนเจ้าของหอ (/partner/notifications) จัดใหม่ตามดีไซน์ Hopak Partner Panel
- แท็บกรอง 5 อัน: ทั้งหมด / การจอง / การเงิน / ข้อความ / เตือน (active เขียว) + ตัวนับต่อหมวด
- การ์ดแยกสีตามหมวด: แถบ accent ซ้าย (ถ้ายังไม่อ่าน) + ไอคอนสี + title + body + เวลา + ปุ่ม action
- map type→หมวด: payout/payment→การเงิน(เขียว,ดูสลิป), booking→การจอง(ฟ้า,ดูการจอง→/partner/requests), chat/message→ข้อความ(ม่วง), warning/dorm_rejected/อื่นๆ→เตือน(ส้ม); dorm_rejected มีปุ่ม "ไปแก้ไข"→dashboard
- คง markRead, ดูสลิป (attachment), realtime notification:new
- tsc web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)
หมายเหตุ: หมวด "การจอง/ข้อความ" จะว่างถ้าเจ้าของหอไม่ได้รับ type นั้น (ตอนนี้ owner ได้ payout/warning/dorm_rejected) — ถ้าอยากให้มีแจ้งเตือน "คำขอจองใหม่" ตอน tenant จอง ต้องเพิ่ม notify owner ใน bookings.create (ยังไม่ทำ)

[87] แดชบอร์ดเจ้าของหอ (/partner/dashboard) ทำ responsive มือถือ จัดการข้อมูลง่าย
- KPI: มือถือ 2 คอลัมน์ (grid-cols-2) แทน 1 คอลัมน์ยาว, gap แคบลง (gap-3 sm:gap-18)
- การจองล่าสุด: จอใหญ่=ตาราง, มือถือ=การ์ดต่อรายการ (เลิกตาราง min-w-720 ที่เลื่อนข้าง)
- income CTA: มือถือ stack แนวตั้ง + ปุ่มเต็มกว้าง (เดิมแถวเดียวล้น)
- การ์ดใหญ่: padding p-4 บนมือถือ → sm:p-6 (revenue/occupancy/recent/todo)
- tsc web ผ่าน (screenshot partner ติด headless-auth เตะ login — ไม่ใช่บัคหน้า)
สถานะ: แก้แล้ว (30/07/2026)

[88] ใบเสร็จ/ใบจอง A4 พิมพ์ได้ (DesignSync Hopak Receipt) — 2 ฉบับ ผู้เช่า(มีรหัส)/เจ้าของหอ(ไม่มีรหัส)
- components/Receipt.tsx: sheet A4 (header H+HOPRAK.COM+PAID, No./วันที่, ข้อมูลผู้จอง, ห้องพัก, ชำระเงิน+ยอดรวม, รหัสยืนยัน[เฉพาะฉบับผู้เช่า], เงื่อนไข, ลายเซ็น) + toolbar พิมพ์/PDF (window.print) + @media print A4
- โชว์รหัสยืนยัน iff booking.checkInToken มีค่า — findOneFor strip token ให้ non-tenant → เจ้าของหอ/admin ได้ฉบับไม่มีรหัสอัตโนมัติ
- หน้า tenant /bookings/[id]/receipt + owner /partner/receipt/[id] (ทั้งคู่ fetch /bookings/:id)
- ปุ่มเข้า: tenant booking detail (paid) "พิมพ์/ดาวน์โหลดใบเสร็จ" ; owner /partner/slips row "ดาวน์โหลด PDF"→/partner/receipt/[id]
- ฟิลด์ที่ระบบไม่มี (บัตรปชช/อีเมล/ที่อยู่/ฉุกเฉิน/ที่จอดรถ) เป็นเส้นประกรอกมือ (ตาม intent form)
- เทส/verify: screenshot ฉบับเจ้าของหอ (admin) render ตรงดีไซน์ ไม่มีรหัส ; tsc web ผ่าน
สถานะ: แก้แล้ว (30/07/2026)

[89] ผู้เช่าส่งคำขอจอง แต่ไม่มีแจ้งเตือนเข้าเจ้าของหอ
สาเหตุ: bookings.create ส่งแค่ socket 'booking:new' ชั่วคราว ไม่ได้สร้าง notification ถาวร เจ้าของหอเลยไม่เห็นในกระดิ่ง/หน้าแจ้งเตือน
แก้: bookings.create เพิ่ม notifications.create(ownerId, 'booking', 'คำขอจองใหม่', body) — body มีชื่อผู้เช่า/ประเภทห้อง/วันเข้าอยู่/ระยะเวลาเช่า
- type 'booking' → หน้าแจ้งเตือนเจ้าของหอจัดเป็นหมวด "การจอง" (ฟ้า) + ปุ่ม "ดูการจอง"→/partner/requests
- emit 'notification:new' realtime อยู่แล้วใน notifications.create → กระดิ่ง+หน้าแจ้งเตือนเด้งทันที
เทส: register tenant → POST /bookings ห้อง cms1njh6y (owner@hopak.com) → owner ได้ notif "คำขอจองใหม่" (verify DB) ; tsc api ผ่าน ; ลบ test data
หมายเหตุ: การจองที่ผู้เช่าส่งก่อนแก้ ยังโผล่ใน /partner/requests (หมวดการจอง) หลังรีเฟรช แต่ไม่มี notif ย้อนหลัง
สถานะ: แก้แล้ว (30/07/2026)

[90] Stored XSS ผ่านไฟล์อัพโหลด (SECURITY, ระดับสูง)
ที่มา: upload endpoint เกือบทั้งหมด (avatar, become-owner docs, rooms/dorms photos, admin approvals photos, admin-users docs, payments/finance slip, owner-applications photo/doc) ไม่มี fileFilter เลย — อัพ .svg/.html ยัด <script> ได้ ; key = `${Date.now()}-${file.originalname}` เก็บนามสกุลไฟล์ตามที่ผู้ใช้ตั้ง ; express.static (/uploads/) เสิร์ฟ inline เป็น image/svg+xml หรือ text/html → สคริปต์รันบนโดเมน API
ซ้ำ: settings hero/poster/area มี filter แต่ใช้ mimetype.startsWith('image/') ซึ่ง image/svg+xml ก็ผ่าน
แก้ 2 ชั้น:
- ชั้น 1: common/upload-filters.ts (ใหม่) — imageFileFilter (jpg/jpeg/png/gif/webp) + documentFileFilter (+pdf) whitelist ทั้งนามสกุลและ MIME พร้อมกัน ; ใส่ทุก interceptor + size limit ; แทน filter อ่อนใน settings
- ชั้น 2: main.ts static serve /uploads/ ตั้ง X-Content-Type-Options: nosniff + CSP "default-src 'none'; sandbox" ; files.controller (private) ตั้ง header เดียวกัน ; uploads.service.upload() กัน path traversal (reject key มี '..' หรือ '\')
เทส: tsc api ผ่าน (exit 0)
สถานะ: แก้แล้ว (03/08/2026)
